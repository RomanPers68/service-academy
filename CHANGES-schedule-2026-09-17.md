# График и виброотдача — правки от 17.09.2026

Правки точечные. Затронуты три файла: `ui/schedule.jsx`, `ui/css.js`,
`lib/utils.js`. Логика генератора, формат данных и обмен с сервером
не тронуты — расстановка, замки, факт часов и зарплата считаются как раньше.

---

## 1. `ui/schedule.jsx` — «ПОКРЫТИЕ 0 %» при полной таблице (баг)

Значение покрытия писалось в `covTarget.current` в теле вида менеджера,
то есть **после** раннего выхода `if (state === "load") return`. На монтировании
эффект анимации стартовал на пустых данных (0 → 0), а когда график приезжал
с сервера, перезапускать эффект было нечем: в зависимостях стояли только
`genKey`, `mkey`, `weekIdx`. Цифра навсегда оставалась нулём (на записи экрана
видно `ПОКРЫТИЕ 0%` и `8%` при живых 1/1, 5/5 в таблице).

Теперь покрытие считается в `useMemo` **до** эффекта и попадает в его
зависимости, поэтому цифра догоняет реальность и после загрузки, и после
каждой правки. Ref `covTarget` стал не нужен и удалён.

## 2. `ui/schedule.jsx` — палитра смен вместо перебора по кругу

Тап по клетке перебирал смены циклом `["", ...shifts]`: при трёх сменах
поставить третью — три касания, стереть — четыре, и каждое промежуточное
касание закрепляло клетку замком.

Теперь тап открывает нижний лист с выбором: цвет, буква, название, время
и часы каждой смены плюс отдельная строка «Выходной». Для занятой клетки
там же кнопки «Кто вместо?» и «Факт часов» — те же экраны, что и в режимах,
но без переключения режима. Ставит смену новая функция `setCell(id, d, k)`.

Режимы «Правка · Кто вместо? · Обмен · Факт часов» оставлены как были:
их поведение не менялось, «Обмен» по-прежнему требует двух касаний.

## 3. `ui/schedule.jsx` — ручная правка попала в «Отменить»

`snapUndo()` вызывался только для генерации, обмена, очистки и снятия замков.
Самое частое действие — обычный тап по клетке — страховкой не покрывалось.
Теперь `setCell` снимает снимок перед изменением, и «↩» возвращает последнюю
поставленную смену.

## 4. `ui/schedule.jsx` + `ui/css.js` — имена в одну строку

В колонке шириной 100 px «Каирлинова Анастасия», «Никифоров Никита»,
«Мирослава Олехнович» переносились на вторую строку и удваивали высоту ряда.
На 19 сотрудниках это лишний экран прокрутки.

Добавлен модульный помощник `shortName()` → «Каирлинова А.». Полное имя
осталось в `title` ячейки и во всех остальных местах (аналитика, «Сегодня»,
экспорт картинкой, настройки) — сокращена только сетка. Колонка ужата
до 92 px с `max-width`, добавлены `nowrap` и многоточие.

## 5. `ui/schedule.jsx` — текущий месяц открывается на текущей неделе

`weekIdx` стартовал с `null` («весь месяц»), то есть 30 колонок по 26 px
внутри страницы, которая листается вниз. Новый эффект при открытии текущего
месяца выбирает неделю, в которую попадает сегодня; для других месяцев
остаётся «весь месяц». Ручной выбор не перебивается — эффект срабатывает
только на смену месяца.

## 6. `ui/schedule.jsx` — одна легенда вместо двух

Палитра печаталась дважды: сокращённая над таблицей и полная под ней.
Нижняя удалена, время смен (`11:00–24:00`) перенесено в верхнюю.

## 7. `ui/schedule.jsx` — вид сотрудника: «ближайшая смена» наверх

Строка стояла девятым блоком — после часов, заработка, прошлого месяца,
«+2 смены», достижений, обмена сменами и чек-листа. Ради неё график
и открывают. Поднята сразу под имя и увеличена: день, название смены,
время, часы и старший смены с кнопкой звонка.

## 8. `ui/schedule.jsx` — вид сотрудника: список дней

- Нерабочие дни сжаты (меньше отступы, шрифт даты 14 вместо 17, прозрачность 0.75) —
  смены складываются в различимый ритм вместо тридцати одинаковых строк.
- Подпись выходного была случайной из трёх (`["Выходной", "Отдыхай ✦", "Твой день"][d % 3]`);
  одно состояние — одно слово, «Выходной».
- Пустая строка времени под выходным больше не рисуется (занимала высоту зря).

## 9. `ui/schedule.jsx` — настройки: подпись перед нумерованными разделами

Над разделами 1–6 стоят две карточки-помощника без номера, и список выглядел так,
будто нумерация начинается с третьего пункта. Добавлена строка «шесть шагов настройки».

## 10. `lib/utils.js` — виброотдача в APK (баг)

`vibrate()` проверял `window.Telegram?.WebApp?.HapticFeedback` и, если объект есть,
уходил в ветку Telegram. Но `telegram-web-app.js` подключён в `index.html`
безусловно — он грузится и в APK, и в обычном браузере, и всё равно создаёт
`WebApp.HapticFeedback`. Проверка проходила **всегда**, вызывалась пустышка,
и до `navigator.vibrate` управление не доходило. В Telegram вибрация работала,
в установленном приложении — нет.

Добавлена функция `inTelegram()`: настоящий клиент определяется по непустой
`initData` либо по `platform`, отличной от `"unknown"`. Вне Telegram теперь
честно используется Web Vibration API.

### Важно: одной правки кода может не хватить

`navigator.vibrate()` внутри TWA работает только если в манифесте Android-приложения
объявлено разрешение:

```xml
<uses-permission android:name="android.permission.VIBRATE" />
```

PWABuilder этого разрешения по умолчанию не добавляет. Если после обновления сайта
отдачи в APK всё ещё нет — дело в манифесте, и чинится это при пересборке пакета
(в PWABuilder → All Settings включить исходный код и добавить строку, либо собрать
через Bubblewrap). Проверить просто: та же сборка в обычном Chrome на том же
телефоне должна вибрировать — если вибрирует в Chrome, но молчит в приложении,
причина именно в разрешении.

Отдельно проверь в системных настройках телефона, что включена «вибрация при
нажатии» и телефон не в режиме энергосбережения — Chrome глушит вибрацию в обоих случаях.

---

## Что НЕ делалось (осознанно)

- **Режимы не убраны.** Предлагал свести «Правка · Кто вместо? · Обмен · Факт часов»
  в одно меню у клетки. Палитра закрывает частый случай, но полный демонтаж режимов
  задевает обмен (два касания) и требует проверки на живых данных — без сборки
  и прогона тестов это риск.
- **Замечания не сделаны кликабельными.** Чтобы тап по строке прыгал к нужному дню,
  `audit()` должен возвращать объекты `{ day, staffId, text }` вместо строк,
  а его результат используется ещё и в экспорте картинкой и в подсказках генератора.
  Это отдельная правка, не «заодно».
- **Переработки.** Каирлинова 254/195, Старченко 221/195, Марк Финке 201/195 —
  это +30…+60 часов сверх нормы, и половина полосок в таблице красная. Когда
  красным горит половина команды, красный перестаёт быть сигналом. Но это вопрос
  к настройкам норм или к генератору, а не к интерфейсу, — интерфейс показывает
  честно.

## Проверка

Собрать локально не мог: сети в среде нет, `npm install` недоступен.
Проверено тем, что доступно офлайн:

- `lib/utils.js` — полноценная проверка синтаксиса `node --check` (JSX в файле нет): **ОК**.
- `shortName()` и `inTelegram()` — прогнаны на реальных случаях: «Каирлинова Анастасия» →
  «Каирлинова А.», имя без фамилии, пустая строка; Telegram / APK-пустышка / браузер.
- `ui/schedule.jsx`, `ui/css.js` — баланс скобок и парность JSX-тегов (`div`, `span`,
  `button`, `td`, `tr`, `Sec`) сверены с исходником: расхождений ноль.
- Порядок хуков: все `useState/useEffect/useMemo` остались выше раннего
  `return` при `state === "load"`; после него новых хуков нет.

Перед выкладкой прогони у себя `npm run check:all`.

---

## Дополнение: «морозный лёд» в палитре смен

Новый лист выбора смены изначально унаследовал плоский стиль модалки
«Кто вместо?» (сплошная заливка + золотая рамка). Переведён на общую
рецептуру карточек графика — в обеих темах:

- полупрозрачное стекло вместо сплошного фона;
- свечение изморози изнутри (`inset 0 0 …`);
- блик по верхней кромке (`inset 0 1px 0`) и более светлый `border-top`;
- мягкая тень наружу снизу, чтобы лист «лежал» на экране.

Светлая тема получает тёплое молочное стекло, тёмная — тот же приём
на тёмном янтаре. Выбранная строка подсвечивается золотом, не теряя стекла.

---

# Генератор смен — правки от 17.09.2026 (вторая серия)

Файлы: `lib/schedule-gen.js`, `ui/schedule.jsx`. Жёсткие правила (отпуск,
выходные, цикл 2/2, отдых, «подряд», недельные квоты) не тронуты — весь
`canPlace` как был. Менялись только приоритеты раздачи и оценка расстановки.

## 11. Балансировка по доле СВОЕЙ нормы, а не по числу смен (главное)

Было, в жадном проходе:

```js
if (cnt[a.id] !== cnt[b.id]) return cnt[a.id] - cnt[b.id];   // у кого меньше смен
```

При одинаковых нормах это честно. Но как только в команде появляется
полставочник (студент с нормой 100 против 195 у полной ставки), «равное число
смен» означает для него двойную переработку. Теперь сравнивается доля от
личной нормы с учётом длины предлагаемой смены, с допуском 2 %
(≈4 ч на ставке 195) — внутри допуска решают прежние ключи и джиттер.

Замер на стенде: 6 полных ставок (195 ч) + 3 полставки (100 ч),
потребность 4 чел/день и 5 в пик, среднее по 5 посевам.

|                            |   ДО  | ПОСЛЕ |
|----------------------------|------:|------:|
| загрузка полных ставок     |  96 % | 113 % |
| загрузка полставочников    | 182 % | 113 % |
| разрыв между крайними      |  89 % |  13 % |
| переработка полставок      | 246 ч |  38 ч |
| незакрытых дыр             |   0   |   0   |

Полставочники работали 182 часа при норме 100. Теперь нагрузка легла
пропорционально, покрытие не пострадало.

## 12. Переработка штрафуется в счёте

Оценка учитывала только недоработку (`Math.max(0, norm - hours)`). Перебор
часов не стоил ничего, и раздавать его было выгодно: лишний час одному
уменьшал недобор у другого. Теперь час переработки дороже часа недоработки,
вес правится через `rules.overWeight` (по умолчанию 2.5).

Честно про эффект: **сам по себе этот штраф почти ничего не меняет.** Он
выбирает лучший из 24 прогонов, а если все прогоны одинаково перегружены —
выбирать не из чего. Реально работает правка №11, а штраф — страховка
на случай, когда у мультистарта есть выбор.

## 13. Справедливость пиков считалась неверно

```js
sc += (Math.max(...peaks, 0) - Math.min(...peaks, 0)) * 0.5;
```

Минимум брался по всем, включая отпускников с нулём смен: у человека в отпуске
весь месяц `peaks = 0`, и разброс превращался в «максимум минус ноль» — метрика
ничего не мерила. Теперь считаются только реально работавшие (порог 4 смены,
тот же, что у справедливости типов смен).

## 14. Генератор стал воспроизводимым

Внутри стоял `const seed = (Date.now() & 0xffff)` — каждое нажатие «Заполнить»
давало другой график. Менеджер жал дважды, видел разные расстановки и переставал
понимать, думает машина или бросает кости.

Теперь посев приходит снаружи, из месяца: `(Y*12 + M)*7919 + mix*104729`.
Один месяц с теми же настройками — один и тот же результат. Другой вариант
запрашивается явно: в «Ещё» появилась кнопка **«Перемешать»** (сдвигает посев,
дальше обычное «Заполнить черновик»).

Проверено: при запасе людей (12 человек на потребность 5–6) восемь посевов дают
семь разных графиков. При тесном штате все посевы сходятся в один — и это
правильный ответ: другого хорошего расклада просто нет.

## Проверка второй серии

- `lib/schedule-gen.js` — настоящая проверка синтаксиса `node --check`: ОК.
- Все 8 утверждений из `tests/schedule-gen.test.js` прогнаны вручную
  (vitest офлайн недоступен) — прошли.
- A/B-замер: исходный генератор против правленого, одни данные и одни посевы.
- `ui/schedule.jsx` — баланс скобок и парность JSX сверены с исходником.

## Что дальше, если захочешь

Самое крупное из оставшегося — **локальный поиск после ремонта**. Сейчас
`repair()` чинит только дыры, хотя `score(pm)` и `canPlace` уже написаны:
цикл «возьми случайную смену, попробуй передать другому или переставить на
другой день, посчитай счёт, оставь если лучше» на тысяче итераций занимает
доли секунды и чинит переработки, несправедливые вечера и нарушенные
пожелания разом. Мультистарт на 24 прогона — это перебор вслепую;
локальный поиск на порядок эффективнее той же ценой.

---

## 15. Уволенные получали смены (баг, вероятно заметный)

Метка `till` — последний рабочий месяц человека. Интерфейс её учитывал:

```js
const staff = staffAll.filter(q => !q.till || mkey <= q.till);   // ui/schedule.jsx
```

Но в генератор уходил **сырой `cfg`**, а внутри стояло просто `cfg.staff || []`.
Уволенный в июле продолжал получать смены в сентябре.

Последствие хуже, чем кажется. В таблице его строки нет — она рисуется по
отфильтрованному списку. Зато слот дня он занимал: генератор считал потребность
закрытой и шёл дальше, а на экране день выглядел недобранным, и объяснить эту
дыру было нечем. Часть «не хватает 14 смен» могла быть именно этим.

Проверено на стенде: до правки уволенный в июле получал 20 смен в сентябре,
после — ноль.

Фильтр перенесён внутрь генератора (он и так знает `mkey`), поэтому чинятся
все пять мест вызова разом, а не только «Заполнить черновик».

### Заодно проверено, что остальные ограничения работают

Один месяц, восемь человек, все ограничения сразу — все проверки прошли:

- уволенный не получил ни одной смены;
- отпускник свободен 10–20 и при этом работает вне отпуска;
- постоянный выходной по воскресеньям соблюдён;
- разовые выходные конкретными датами (5 и 6) соблюдены;
- «не смогу выйти» (12, 13, 14) соблюдено;
- дыр при этом ноль.

Все пять — жёсткие правила внутри `canPlace`: ни жадный проход, ни ремонт
их не обходят. Пожелания о выходных (`wishes`) — мягкие: уважаются, пока не
ценой дыры в зале.

---

# Экран пожеланий сотрудника — 17.09.2026 (третья серия)

Файл: `ui/schedule.jsx`. Серверная часть и формат данных не тронуты — всё
работает через существующий `setWish(staffId, d, on, kind)`, те же квоты,
та же синхронизация, тот же `hardCapacity`.

## 16. Пожелания за одно действие вместо тридцати раскрытий

Кнопки «Попросить выходной» и «Не смогу выйти» существовали и раньше, но
лежали **внутри раскрытого дня**. Чтобы отметить отпуск с 10 по 20, человек
делал двадцать два касания: одиннадцать раскрытий плюс одиннадцать нажатий.
Проще было написать менеджеру в мессенджер — так все и делали, а менеджер
вбивал это руками. Вот где уходило время, а не в генераторе.

Добавлена карточка «Мои пожелания» над списком дней:

- **весь месяц одной сеткой**, 7 колонок, с понедельника, выровнено по дням недели;
- состояние каждого дня читается цветом: свободен, прошу выходной (золото),
  не смогу (красный), уже смена, отпуск, постоянный выходной, прошедший день;
- **тап по дню** открывает лист с выбором — просьба, «не смогу», убрать отметку
  (тот же «морозный лёд», что у палитры менеджера, в обеих темах);
- **«Отметить период»** — два тапа на первый и последний день, дальше один выбор
  на весь диапазон;
- сверху счётчик: сколько просьб и сколько «не смогу» уже отмечено.

Отметка периода идёт циклом через тот же `setWish` и честно отчитывается:
дни со сменой, отпуском или постоянным выходным пропускаются, а если на день
кончилась квота «не смогу» — это попадает в итог отдельной цифрой
(«отмечено дней: 8 · на 3 — мест уже нет»). Тихо проглотить часть диапазона
было бы хуже всего.

## 17. Срок приёма пожеланий

В «Правилах смен» появилось поле «пожелания принимаем до числа» (0 — без срока).
Сотрудник видит его на своём экране: «Принимаем до 25 числа — после график
собирается». После этой даты сетка текущего месяца закрывается для правок
и объясняет почему.

Это же поле — опора для будущей автосборки: собирать месяц имеет смысл
после закрытия приёма, а не по пустым пожеланиям.

## 18. У сотрудника не рисовалась строка состояния

`setMsg()` вызывался и в ветке сотрудника, но сам `msg` выводился только в виде
менеджера. Любой ответ системы сотруднику уходил в пустоту. Добавлена строка
состояния — без неё отчёт об отметке периода был бы невидим.

## Проверка третьей серии

- баланс скобок и парность JSX сверены с исходником: расхождений ноль;
- каждый использованный идентификатор (`IcoSun`, `IcoBan`, `hardCapacity`,
  `frozenBefore`, `isDayOff`, `setWishRangeApply`) проверен на объявление;
- `wishDeadline` добавлен в `DEFAULT_CONFIG.rules` со значением 0,
  чтобы старые сохранённые настройки открывались без него корректно;
- проверки генератора из второй серии прогнаны заново — проходят.

## Что осталось до «настроил один раз и работает»

1. **Поля «работает с»** нет — есть только «работает до». Новенького приходится
   заводить вручную в нужный момент, иначе он получит смены задним числом.
   Поле симметричное, добавляется просто.
2. **Автосборка месяца** после закрытия приёма пожеланий. Теперь она безопасна:
   генератор детерминированный, посев берётся из месяца.
3. **Чистка главного экрана менеджера** — ряд режимов в «Ещё», покрытие в
   карточку «Сегодня», легенда под знак вопроса.

---

# Три шага до «настроил один раз» — 17.09.2026 (четвёртая серия)

Файлы: `ui/schedule.jsx`, `lib/schedule-gen.js`.

## 19. Поле «работает с»

Было только «работает до» (`till`). Новенького приходилось заводить ровно в
нужный момент, иначе он получал смены задним числом — в месяцах, когда его
ещё не было. Добавлено симметричное поле `since` (формат `ГГГГ-ММ`):

- в редакторе сотрудника, рядом с нормой;
- в фильтре `staff` в интерфейсе;
- в фильтре внутри генератора — значит, во всех пяти местах вызова.

## 20. Автосборка черновика

Раньше генератор ждал нажатия, и это было правильно: он брал `Date.now()`
и каждый раз выдавал другой график — автозапуск пугал бы. После перехода на
посев из месяца результат воспроизводим, поэтому собирать можно заранее.

Переключатель «Собирать сам» в «Правилах смен», по умолчанию **выключен**.
Условия намеренно строгие, чтобы автоматика никогда не спорила с человеком:

- включено в настройках;
- месяц полностью пуст — ни одной расставленной смены;
- нет несохранённых правок;
- месяц не прошлый;
- срок приёма пожеланий закрыт (или срока нет);
- один раз на месяц за сессию (`autoDone` ref).

Результат — **черновик**. На сервер по-прежнему уходит только по «Сохранить».

## 21. Главный экран менеджера разгружен

- **Ряд из четырёх режимов убран целиком.** «Кто вместо?» и «Факт часов» живут
  в листе по тапу на клетку, «Обмен» уехал в «Ещё». Когда обмен включён, сверху
  висит строка состояния с выходом — режим больше не скрытый.
- **Покрытие месяца переехало** из шапки таблицы в карточку «Сегодня». Дыры
  показывались четырьмя способами сразу; осталось два — числа в строках позиций
  (они в контексте дня) и одна сводка там, где менеджер и так ищет «что горит».
- **Легенда — под кнопкой** «что означают буквы?». Стояла на экране каждый день
  и вытесняла таблицу вниз.
- **Подсказка под чипами недель** сокращена: про режимы она больше не нужна.

Мёртвый код убран: состояния `repl` и `factMode` и их ветки в `tapCell`
удалены — включать их стало нечем.

---

# Аудит перед заливкой

Собрать проект офлайн нельзя (нет сети, `npm install` недоступен), поэтому
проверял тем, что даёт настоящий результат.

**1. Осиротевшее состояние** — после удаления режимов `setRepl` и `setFactMode`
остались только в объявлениях. Найдено и вычищено вместе с мёртвыми ветками.

**2. Висячие ссылки** на удалённое (`repl`, `factMode`, `covTarget`) — не осталось.

**3. Порядок хуков** — 81 хук до раннего `return` при `state === "load"`,
после него **ноль**. Правило хуков не нарушено.

**4. Объявления** — каждый идентификатор из нового кода (`Pill`, `Text`, `Num`,
`Field`, `IcoSun`, `IcoBan`, `onActivate`, `setLegend`, `setMix`, `setSwap`,
`setSwapSel`, `setPick`, `setWish*`, `autoDone`, `generate`, `frozenBefore`,
`hardCapacity`, `setWishRangeApply`, `setCell`, `shortName`, `cover`)
проверен на объявление в файле.

**5. Синтаксис** — `node --check` на `lib/schedule-gen.js` и `lib/utils.js`
(JSX там нет): оба проходят.

**6. Структура JSX** — баланс скобок ОК; парность `div`, `span`, `button`,
`td`, `tr`, `Sec` даёт ту же дельту, что и у нетронутого исходника.

**7. Поведение генератора** — девять проверок на одном месяце, все прошли:

```
✓ уволенный не получил смен
✓ ещё не принятый (since) не получил смен
✓ отпускник свободен 10–20
✓ отпускник работает вне отпуска
✓ постоянный выходной по воскресеньям соблюдён
✓ разовые выходные 5, 6 соблюдены
✓ «не смогу» 12–14 соблюдено
✓ полставочник не перегружен (78 ч при норме 100)
✓ тот же посев — тот же график
  дыр за месяц: 0
```

**8. Прежние тесты** — все 8 утверждений из `tests/schedule-gen.test.js`
прогнаны вручную (vitest офлайн недоступен): прошли.

## Чего аудит НЕ покрывает

Честно о границах: без сборки я не проверил, что JSX компилируется — баланс
тегов это не гарантирует. Не проверено вживую поведение автосборки (нужен
React-рантайм) и то, как новая сетка пожеланий выглядит на реальном экране.

**Перед заливкой обязательно прогони у себя `npm run check:all`** — там
и сборка, и vitest, и smoke-render. Если что-то всплывёт, пришли текст ошибки.

---

# Почему настройки не сохранялись и автозаполнение молчало

Две причины, обе в коде, обе объясняют симптомы. Файл: `ui/schedule.jsx`.

## 22. Слияние настроек с умолчаниями было поверхностным

```js
setCfg(v ? { ...DEFAULT_CONFIG, ...v.config } : { ...DEFAULT_CONFIG });
```

Спред поверхностный: если в сохранённой настройке есть ключ `rules`, он
заменяет `DEFAULT_CONFIG.rules` **целиком**. Значит любое поле, добавленное
в умолчания позже, отсутствует у всех, кто настраивался раньше.

Прямое следствие: `cfg.rules.autoFill` приходил с сервера как `undefined`,
и эффект автосборки выходил на первой же проверке — **автозаполнение не могло
сработать в принципе**. С `wishDeadline` то же самое: срок приёма пожеланий
не показывался никому.

Добавлена `mergeCfg()` — сливает вложенные объекты (`rules`, `need`, `split`,
`posRules`) по ключам. Применена в обоих местах загрузки: с сервера и из
офлайн-копии.

Проверено на настройке, сохранённой до появления новых полей:

```
✓ новое поле autoFill появилось
✓ новое поле wishDeadline появилось
✓ сохранённое peakDows НЕ затёрто умолчанием
✓ сохранённое maxRow НЕ затёрто
✓ сохранённый need уровня 1 сохранён
✓ недостающий уровень 3 взят из умолчаний
✓ staff не тронут
```

## 23. Отложенное сохранение настроек терялось

```js
saveTimer.current = setTimeout(() => saveCfg(next), 900);
```

Правка применялась мгновенно, а на сервер уходила через 900 мс. Паузу никто
не дожидался: закрыл приложение или переключил месяц в эти секунды — таймер
умирал вместе с компонентом, и настройка пропадала бесследно. Так и выглядело
«настройки не сохраняются»: переключил, вышел, вернулся — всё как было.

Отдельно: переключение месяца вызывает `load()`, а тот делает `setCfg()`
ответом сервера. Свежая правка визуально откатывалась ещё до того, как
успевала уйти.

Теперь несохранённое лежит в `pendingCfg` и дописывается принудительно:

- при уходе со страницы (`pagehide`);
- при сворачивании приложения (`visibilitychange` → hidden);
- при размонтировании компонента;
- перед любой перезагрузкой месяца.

## Про автозаполнение: условия остались строгими

Даже после починки автосборка срабатывает только если месяц **полностью
пуст**. Для текущего месяца, где смены уже расставлены, она не сработает
никогда — и это задумано, чтобы автоматика не спорила с человеком.
Если ожидается другое поведение (например, дозаполнять дыры в уже начатом
месяце), это отдельное решение и отдельная правка.

---

# Заполнение по должностям — 17.09.2026 (шестая серия)

## 24. Автосборка работала «всё или ничего»

Условием было «месяц полностью пуст». В живом заведении это не наступает
никогда: менеджер и хостес ходят 2/2 и ставятся заранее, официанты плавающие
и заполняются позже, по ситуации. Автосборка молчала — не потому что сломана,
а потому что условие не соответствовало работе.

Теперь она берёт **только те должности, где не стоит ни одной смены**.
Начатое не трогается. Сообщение называет, что именно собралось:
«Черновик собран автоматически: Официант, Раннер — проверь и сохрани».

## 25. Кнопка «Заполнить» уважает выбранную должность

Чипы должностей над таблицей уже были — теперь они управляют и заполнением.
Выбран «Официант · 9» → кнопка меняет подпись на «Заполнить: Официант»
и ставит смены только официантам. Остальные остаются фоном занятости:
генератор видит их смены при подсчёте потребности, но ни одной чужой клетки
не двигает. Механизм (`onlyIds`) в генераторе уже был — использовался только
для вливания новенького в живой месяц.

### Попутно найдена ошибка, которую я сам чуть не внёс

`onClick={generate}` передаёт в обработчик **событие клика**. После того как
`generate()` начал принимать должность первым аргументом, объект события
приехал бы на место `scopePos`. Заменено на `onClick={() => generate(posFilter || null)}`.

### Проверка на рабочем сценарии

Менеджеры и хостес расставлены 2/2 вручную, официанты пусты, заполняем
только официантов:

```
✓ смены менеджеров не изменились
✓ смены хостес не изменились
✓ официанты заполнены (17–18 смен каждому)
✓ дыр не осталось
```

## Что осталось негибким: отпуск

`s.vac = [начало, конец, месяц]` — **одна запись на человека**. Поставишь
отпуск на октябрь — сентябрьский пропадёт, потому что поле одно и оно
перезаписывается. Для «в разные месяцы разные причины» этого мало.

Правильно было бы хранить отпуска списком по месяцам:
`s.vacs = { "2026-09": [[10,20]], "2026-10": [[1,5]] }` — тогда и несколько
периодов в одном месяце станут возможны. Это меняет формат данных и трогает
шесть мест (`onVac`, `effNorm`, `vacOn`, редактор, генератор, экспорт),
поэтому отдельной правкой, не «заодно».

---

# Сегодняшний столбец — 17.09.2026 (седьмая серия)

## 26. «Сегодня» в таблице: скобки заменены на столб света

Было — на каждой клетке столбца:

```js
boxShadow: `2px 0 0 ${GOLD} inset, -2px 0 0 ${GOLD} inset`
```

Два жёстких золотых рельса по 2 px. На экране это читалось не как выделенный
день, а как пара скобок, рассекающих таблицу сверху донизу: рельсы проходили
сквозь строки должностей, спорили с разделителями недель и обрывались внизу
ни на чём — столбец просто кончался в воздухе.

Стало — единый помощник `todayCol(role)` с четырьмя состояниями:

- **head** — шапочка: вертикальный градиент, скругление `9px 9px 0 0`,
  блик по верхней кромке;
- **group** — строка должности, заливка чуть плотнее;
- **cell** — клетки сотрудников, самая лёгкая заливка;
- **last** — донышко: скругление `0 0 9px 9px` и грань снизу, чтобы столбец
  был закрыт.

Вместо рельсов — грань в 1 px с прозрачностью 0.38–0.42 и свечение изнутри
(`inset 0 0 14px`), та же «морозная» рецептура, что у карточек и палитры.
Обе темы разведены: в светлой блик белый и плотный, в тёмной — тёплый янтарь.

Последняя видимая строка вычисляется с учётом фильтра должностей: выбран один
«Официант» — донышко встанет под последним официантом, а не под всей таблицей.

Инлайн-стили перебивают классы зебры (`.sa-schedzeb td`), так что чередование
строк столбец не съедает — проверено по CSS.

---

# Отпуска по месяцам и разгрузка настроек — 18.09.2026 (восьмая серия)

## 27. Отпуск: список по месяцам вместо одного поля

Было `s.vac = [с, по, месяц]` — **одна запись на человека за всё время**.
Поставил отпуск на октябрь — сентябрьский молча пропадал, потому что поле
перезаписывалось. Для «в разные месяцы разные причины» это не годилось.

Стало `s.vacs = { "2026-09": [[10,20],[28,30]], "2026-10": [[1,5]] }`:
каждый месяц независим, периодов в месяце сколько угодно.

- **Старый формат читается** как один период — сохранённые отпуска не пропадут.
- Любая правка переводит человека на новый формат, и при этом **чужой месяц
  из старого поля переносится в `vacs`**, а не теряется.
- Пересекающиеся периоды при расчёте нормы считаются по уникальным дням —
  иначе вычет из нормы удвоился бы.
- В редакторе вместо двух полей «с/по» — список периодов с кнопками
  «+ ещё период» и удалением, плюс счётчик «всего N дн.».

Помощник `vacRanges()` продублирован в `lib/schedule-gen.js` и `ui/schedule.jsx` —
генератор ничего не импортирует из интерфейса и тестируется в Node отдельно.

### Проверка на восьми сценариях

```
✓ старый формат vac читается
✓ старый формат не влияет на другой месяц
✓ первый период сентября соблюдён
✓ второй период сентября соблюдён
✓ между периодами человек работает
✓ октябрьский период соблюдён
✓ сентябрьские периоды в октябре НЕ действуют
✓ пересекающиеся периоды не удваивают вычет
  дыр: сент 0 · окт 0
```

## 28. Настройки: справка убрана под кнопку

На записи экрана видно, что «Правила смен» — стена: срок пожеланий,
автосборка, пиковые дни, высокие дни, три числа, праздники, режим нормы,
**два абзаца курсива**, потом семь строк «как выходит каждая позиция».
В «Сотрудниках» внизу ещё один абзац на пять строк.

Добавлен компонент `Hint` — сворачивает длинное пояснение под строку
«как это работает? ▾». Текст никуда не делся, он нужен один раз при
знакомстве; на экране постоянно ему не место.

## 29. Помощник по норме поднят над списком

Блок «Норма сентября при полной ставке — 176 ч» с кнопками «поставить всем»
стоял **под девятнадцатью карточками сотрудников**. Кнопки относятся ко
всему списку, а чтобы до них добраться, нужно было пролистать весь штат.
Перенесён наверх, над списком.

## Аудит

- баланс скобок и парность JSX сверены с исходником — дельта прежняя;
- `node --check` на `lib/schedule-gen.js`: ОК;
- порядок хуков не нарушен;
- ссылок на старое поле `vac` осталось ровно две, обе — намеренная
  совместимость (чтение старого формата и перенос чужого месяца);
- прогнаны заново: девять проверок ограничений, сценарий заполнения по
  должностям, восемь утверждений из `tests/schedule-gen.test.js`.

---

# «Сколько людей нужно» и разбивка по сменам — 18.09.2026 (девятая серия)

## 30. Мёртвые колонки: числа, которые никогда не применяются

На скриншоте владельца раздел «Правила смен» подписан `пик: —`, то есть
**пиковые дни недели не назначены вообще**. При этом в «Сколько людей нужно»
в колонке ПИК стоят реальные числа: Официант 6, Раннер 2. И в разбивке по
сменам заполнен уровень «пик · 6 чел».

Всё это сохраняется и **никогда не используется**: `lvlOf(d)` возвращает 3
только для дней из `peakDows` (или праздников при `holidayPeak`). Нет таких
дней — нет и уровня. Человек настраивает то, что не работает, и узнать об
этом неоткуда: расшифровка уровня живёт в другом разделе.

Под шапкой таблицы добавлена строка:

```
пик: пт, сб · высокий: ср, вс · обычный: остальные дни · праздники считаются пиком
```

А если уровень пуст, но числа в его колонке есть — предупреждение:
«Колонка "пик" сейчас ни на что не влияет: таких дней в месяце нет.
Назначь дни недели в разделе "Правила смен"».

## 31. Разбивка по сменам: свёрнута по должностям

Раскрытыми были **все** должности сразу. При «по типу дня» это пятнадцать
числовых полей на одну должность (пять смен × три уровня), почти все нули,
и на узком экране К и Г переносятся на вторую строку. Отсюда и ощущение
стены.

Теперь должность — одна строка со сводкой, разворачивается по тапу:

```
Официант   по типу дня · об 1У+3Д · выс 3Д+1В · пик 3Д+1В  ▾
Раннер     все в основную смену  ▾
```

## 32. Сходимость с потребностью больше не считается в уме

Подписано «5 чел», сумма по буквам — 4. Что произойдёт с пятым, объяснял
только абзац справки внизу раздела. Теперь под каждым уровнем стоит итог:

- `расписано 4 из 5, остальные 1 — в основную смену`
- `расписаны все 5`
- `весь день в основную смену`
- `расписано 7 — больше потребности!`

## 33. Справка по разбивке — под кнопку

Последний оставшийся абзац курсива тоже свёрнут в `Hint`.

## Поправка к прошлому разбору

В прошлый раз я назвал «сеткой из нулей» раздел «Сколько людей нужно» —
это была ошибка: сам он простой, строка на должность и три числа. Сетка
с нулями лежит в разделе «Смены», в подразделе «разбивка по сменам».
Правки выше сделаны там, где проблема на самом деле.

---

# Два уровня загрузки вместо трёх — 18.09.2026 (десятая серия)

## 34. «Высокий» и «пик» слиты в один уровень

Замечание владельца: два названия для одного смысла — «людей нужно больше
обычного» — путают того, кто настраивает. И это подтверждается его же данными:
Официант 5/5/6, Раннер 1/1/2, остальные 1/1/1 и 2/2/2. **«Высокий» нигде
не отличается от «обычного»** — уровень есть, разницы нет.

Теперь по умолчанию уровня два: **обычный** и **пиковый**. Третий включается
переключателем в «Правилах смен» — для тех, кому нужна разница между
«четверг чуть плотнее» и «суббота забита».

### Ничего не пропадёт у тех, кто третьим пользуется

Число уровней **определяется по данным**, а не навязывается: если хотя бы
у одной должности число в «высоком» отличается и от обычного, и от пика —
третий уровень включается сам. Явный выбор в настройках перебивает
автоопределение в обе стороны.

Я чуть не сломал это сам: сначала добавил `levels: 2` в `DEFAULT_CONFIG.rules`,
а после глубокого слияния (правка №22) умолчание пришло бы **всем**, включая
тех, у кого третий уровень настроен. Поле из умолчаний убрано, работает
автоопределение.

### Что поменялось в коде

- `LVLS` — один список уровней на все экраны: потребность, разбивка по сменам,
  расшифровка дней, сводки разделов. Раньше `[1,2,3]` было зашито в пяти местах,
  и они могли разъехаться.
- `lvlOf` не возвращает 2 при двух уровнях, а день, вручную помеченный
  «высоким», читается как пик — иначе он попал бы на уровень, которого
  на экране нет.
- Переход на два уровня гасит «высокие дни»: иначе день остался бы на
  мёртвом уровне, и задать для него потребность было бы нечем.
- Правка потребности «обычного» при двух уровнях синхронно пишет и в
  «высокий» — чтобы данные не разъехались, если третий уровень включат позже.
- «Пик» переименован в «пиковый» — согласовано с расшифровкой дней.

### Проверка на восьми сценариях

```
✓ у владельца определяется ДВА уровня
✓ где «высокий» отличается — определяется ТРИ
✓ явные два перебивают автоопределение
✓ явные три перебивают автоопределение
✓ при двух уровнях уровень 2 не встречается
✓ пиковый день всё ещё определяется
✓ при трёх уровнях «высокий» возвращается
✓ праздник остаётся пиком
```

---

# Справедливость типов смен — 18.09.2026 (одиннадцатая серия)

## 35. Одному человеку доставалось 3–4 утра подряд

Жалоба владельца. На ровных данных не воспроизвелась: восемь официантов без
ограничений дают максимум две одинаковые смены подряд и перекос по типам 9 %.
Воспроизвелась, когда в стенд добавили реальную жизнь — постоянные выходные
по дням недели, отпуск, «не смогу выйти»:

```
самая длинная серия одной смены подряд: 4
случаев «3 подряд одинаковых» за месяц:  5.4
перекос по типам смен (доля, среднее):   23%
```

Причина в том, что **справедливость считалась только по сумме за месяц**, а на
вчерашний день не смотрел никто. Когда пул кандидатов тонкий, слот раз за разом
достаётся одному и тому же человеку. Для денег это ощутимо: утро 6 ч против
дня 13 ч — три утра подряд стоят вдвое дешевле трёх дней.

### Что сделано

1. **Ключ сортировки помнит вчера.** При раздаче слота считается, сколько дней
   подряд человек уже выходит именно в эту смену, и слот уходит тому, у кого
   серия короче.
2. **Счёт мерил только самый поздний тип смены** («никто не живёт в вечерах»).
   Но перекос по утрам бьёт по деньгам ровно так же. Теперь спред доли
   считается по каждому авто-типу внутри позиции.
3. **Штраф за серии в счёте**: третья одинаковая подряд и каждая следующая.
   Ключ сортировки гасит серии при раздаче, счёт добивает на мультистарте.

### Результат на том же стенде

|                                | ДО  | ПОСЛЕ |
|--------------------------------|----:|------:|
| самая длинная серия            |  4  |   1   |
| случаев «3 подряд» за месяц    | 5.4 |   0   |
| перекос по типам смен          | 23% |  18%  |
| незакрытых дыр                 |  0  |   0   |

Покрытие не пострадало, разброс часов не изменился (он держится
полставочником и отпускником — так и должно быть).

### Поправка: основная смена из правила исключена

Первая версия чередовала **все** типы смен, включая «День» — и это было
неверно. День 11:00–24:00 это полная смена, обычный рабочий день; ходить
в неё несколько дней подряд нормально, и заставлять человека прыгать
с полного дня на утро и обратно — не справедливость, а вред.

Чередуются только короткие смены — утро и вечер: они отличаются и часами
(6 и 7 против 13), и временем начала. Основная смена (`cfg.dayShift`)
выведена и из ключа сортировки, и из штрафа в счёте.

### Результат на том же стенде

|                                  | ДО  | ПОСЛЕ |
|----------------------------------|----:|------:|
| подряд утро/вечер одного типа    |  4  |   2   |
| случаев «3 подряд» за месяц      | 5.4 |   0   |
| подряд полных дней               |  4  |   4   |
| перекос по типам смен            | 23% |  22%  |
| незакрытых дыр                   |  0  |   0   |

Полные дни идут подряд как и раньше — до `maxRow`, это норма, а не перекос.

### Остаточный перекос структурный

Разброс по утрам (у кого-то 2, у кого-то 5) усилением веса справедливости
почти не лечится: пробовал 3 → 12 → 40, перекос сдвинулся 22 % → 22 % → 20 %,
а вес 40 начинает спорить со штрафом за нарушенные пожелания. Причина не в
алгоритме, а в доступности: у людей разные постоянные выходные, отпуска и
«не смогу», и в конкретный день на утро физически некому больше выйти.
Оставлен осторожный вес 3.

---

## 36. Две дыры в правиле чередования (правка защитная, не доказанная)

Правило «не давать третью одинаковую смену подряд» я добавил только в
основной ключ сортировки. Мимо него шли два пути назначения:

1. **Запасной путь** в жадном проходе: когда основная смена не заходит,
   перебираются остальные типы, и кандидат выбирался сортировкой
   «пожелание → недобор до нормы → джиттер». Ни серии, ни числа таких
   смен там не было.
2. **Ремонт дыр** — выбирал человека через `staff.find(...)`, то есть
   **первого подходящего по порядку в списке**. Никакой справедливости
   вообще: при повторных прогонах слот раз за разом уходил одному и тому же.

Добавлен общий помощник `pickFair(pm, list, d, k)` — очередь по правилам:
пожелание → длина серии этой смены → сколько таких смен уже набрано →
недобор до нормы. Применён в обоих путях. Полный день (`baseK`) из счёта
серий исключён, как и в основном проходе.

**Честно о результате.** A/B на тесном стенде (7 официантов, потребность 4,
отпуск, постоянные выходные, «не смогу», 31 день, 5 посевов) дал одинаковые
числа до и после правки: максимум 2 подряд, ноль случаев «три подряд», ноль
дыр. То есть дыры в логике настоящие, но воспроизвести их выстрел на
доступных данных не удалось. Правка оставлена как защитная: путь без правил —
это мина, даже если она сегодня не сработала.

---

## 37. Найдена настоящая причина серий: порядок ключей

Владелец подтвердил, что жалоба была на **актуальной сборке** — значит
правка №35 проблему не закрыла. И действительно: я сам поставил ключи
в неверном порядке.

```js
if (!sl.flex) {                    // утро/вечер — слот без flex
  if (ea !== eb) return ea - eb;   // у кого МЕНЬШЕ таких смен за месяц
}
if (sl.k !== baseK) {              // и только ПОТОМ — серия
  if (ra !== rb) return ra - rb;
}
```

Справедливость по сумме за месяц решала **раньше** проверки серии. Человек,
вышедший из отпуска с нулём утр, выигрывает этот ключ каждый день подряд —
пока не догонит коллег. До проверки серии дело просто не доходило.

Воспроизведено точно на сценарии владельца (Толкачева, отпуск 1–20 октября,
семь официантов, потребность 5, разбивка 1У+3Д+1В):

```
ДО:     ····················ДДДД·ДВВВ·В     ← три вечера подряд
ПОСЛЕ:  ····················ДДДД·ДВУВ·В     ← вечер, утро, вечер
```

| на сценарии с отпуском         | ДО | ПОСЛЕ |
|--------------------------------|---:|------:|
| самая длинная серия утра/вечера|  3 |   2   |
| случаев «3 подряд» за 5 посевов|  5 |   0   |

Проверка серии поднята **выше** ключа справедливости по сумме. Догонять по
числу смен человек теперь будет через день, а не подряд.

Регрессия: тесный стенд, стенд с ограничениями, отпуска по месяцам,
заполнение по должностям, девять проверок ограничений и восемь утверждений
из `tests/schedule-gen.test.js` — всё прежнее, дыр ноль.

### Чему это меня научило

Правка №35 была проверена на стендах, где все накапливали смены равномерно —
и потому выглядела рабочей. Случай «вернулся из отпуска с нулём» такие стенды
не покрывали. Замер без сценария, ломающего гипотезу, доказывает только то,
что код не падает.

---

# Карточка «Сегодня»: состав смены — 19.09.2026

## 38. Три разные формы в одной строке

Строка состава выглядела так:

```
[📞 Старченко] (Д),  [📞 Малахов] (В),  Мирослава (Д)
```

— капсула с именем, потом буква смены **голым текстом снаружи**, потом
запятая. А у кого нет телефона (Мирослава), имя шло вообще без капсулы.
Три разные формы в одном ряду, и строка от этого рваная.

Стало: **одна капсула на человека**, буква смены внутри неё и в своём цвете
(тот же `SHIFT_COLORS`, что в таблице — У синяя, Д золотая, В сиреневая).
Разделяет капсулы воздух, а не запятые: `flex-wrap` с зазором 5 px.

- `CallName` принимает необязательный `badge` — он встаёт внутрь капсулы.
- Без телефона рисуется **та же капсула**, только без трубки и с более
  тихой рамкой: форма важнее, чем наличие звонка.
- Прочие шесть мест вызова `CallName` не передают `badge` и работают как
  раньше — поведение «без телефона просто текст» сохранено для них.
- Имена приведены к `shortName()`: было `x.name.split(" ")[0]`, то есть
  первое слово — «Старченко» у одного и «Кристина» у другого, вперемешку
  фамилии и имена.

## 39. Покрытие 81 % больше не кричит красным

Цвета было два: ≥95 % зелёный, всё остальное тревожный. 81 % — рабочая
середина, а не авария. Добавлена средняя ступень: ≥95 зелёный,
≥75 золотой, ниже тревожный.

## 40. Свёрнутая карточка «Сегодня»: чипы вместо сплошной строки

Было одной строкой: «Менеджер 1/1 Хостес 1/1 Бар 1/2! Барбек 1/1 Официант 5/4
Раннер 1/1 тап — кто в смене». Недобор приходилось выискивать в потоке текста,
а единственным его признаком был восклицательный знак.

Стало: чип на позицию, три состояния цветом —

- **недобор** — тревожная заливка, рамка и полужирные цифры;
- **перебор** — тихая золотая (раньше был неотличим от нормы);
- **закрыто ровно** — только контур.

Восклицательный знак убран: заливка говорит то же самое, а знак добавлял шум.
Подсказка «тап — кто в смене» вынесена на свою строку — раньше она висела
в конце потока позиций и читалась как ещё одна позиция.

---

# Карточка «Сегодня» и печать смены — 19.09.2026

## 41. Раскрытый вид: лица вместо списка

Список «должность: капсула, капсула» отвечал на вопрос «сколько», но не на
«кто» — фамилии сливались в поток. Теперь сетка кружков с инициалами:

- **буква смены** — значком в углу кружка, цветом смены;
- **должность** подписана под именем, это заодно разводит совпадающие
  инициалы (Старченко и Степанян оба «С»);
- **старший** выделен золотым кольцом и подписью — раньше он был только
  в шапке, и связать его с лицом приходилось глазами;
- **дырка** — пунктирный кружок в том же ряду. Раньше она существовала
  только цифрой `1/2` слева, и ряд людей выглядел законченным;
- тап по лицу раскрывает телефон.

## 42. Свёрнутый вид: миниатюра раскрытого, по времени прихода

Было сплошной строкой «Менеджер 1/1 Хостес 1/1 Бар 1/2! …». Стало — те же
лица, только мельче и внахлёст, **сгруппированные по часу начала смены**.

Это показывает то, чего счёт по должностям не показывал вовсе: в 11:00
людей пятеро, а в 17:00 всего двое. Свернуть и развернуть теперь —
приближение, а не смена метафоры.

Больше шести лиц в группе сворачиваются в «+N». Дырки идут пунктирными
кружками в первой группе, под ними строка «не хватает: бар».

## 43. Печать: то же, но читаемое в чате

Было списком: должность, под ней строки со значком смены, именем и часами.
Стало то же, что в приложении, — лица по времени прихода.

Отличие от экрана намеренное: **под кружком полная фамилия и часы**.
В чате картинку читают, а не узнают по инициалам, и тапнуть по ней нельзя —
значит всё нужное должно быть нарисовано. Старший подписан «СТАРШИЙ»
вместо часов, дырки — пунктирными кружками, внизу строка «Не хватает: бар».

Высота холста теперь считается по числу рядов, а не фиксирована: смена из
четырёх человек не оставляет полэкрана пустоты, смена из пятнадцати не
обрезается.

## 44. Номер телефона разваливался на четыре строки

Я положил ссылку с номером **внутрь колонки шириной 60 px** — и она
переносилась внутри кружка: «+7 / (913) / 756-43- / 49». На записи экрана
видно, что выглядит это то кругом в столбик, то полоской.

Номер вынесен отдельной строкой **во всю ширину** под сеткой лиц: имя слева,
золотая кнопка с номером справа, всё в одну строку и без переносов.

## 45. Нажимается вся карточка, а не только кружок

Тап работал только по самому кружку — по имени и должности не срабатывал,
и было непонятно, куда целиться. Теперь обработчик на всей ячейке, выбранная
подсвечивается заливкой, а должность под именем становится золотой.

Раскрытый номер закрывается вместе с карточкой: иначе он оставался висеть,
когда лица уже свёрнуты.

## 46. Блок покрытия переписан

Было два ряда капсом моноширинным — «МЕСЯЦ ЗАКРЫТ НА 81%» и «НА ЭТОЙ НЕДЕЛЕ
не хватает 14 смен, тяжелее всего чт 17 (−3) · показать неделю ›». Плотный
блок кричащих заголовков спорил с лицами над ним.

Стало: тонкая полоса покрытия вместо заголовка — процент видно, не читая, —
и одна спокойная строка про неделю строчными буквами. Цвет полосы тот же,
что у числа: зелёный от 95 %, золотой от 75 %, тревожный ниже.

## 47. Карточка раскрывалась только крошечной стрелкой

Тап работал по шапке — фактически по значку ▾ размером в несколько пикселей.
В него неудобно целиться, и по виду вообще не понять, что карточка
раскрывается. Теперь тап по всей площади свёрнутой сводки раскрывает её.

## 48. «Лист A4» подменял карточку смены таблицей месяца (баг)

Кнопки «Для чата / Лист A4» в превью всегда звали `exportImage` — то есть
рисовали **месячный график**. Человек открывал карточку смены, нажимал
«Лист A4» и получал вместо неё таблицу на весь месяц. На записи экрана это
и видно.

Теперь у картинки есть `kind`, и `redrawShot` перерисовывает **тот же вид**
в другом формате. Заголовок превью, подсказка под ним и имя файла тоже
разведены: «Смена картинкой» против «График картинкой».

## 49. Печатный вид карточки смены

Тёмный фон хорош в приложении, но на бумаге съедает тонер и делает подписи
нечитаемыми, а такую картинку вешают на кухне. Добавлена вторая палитра:

- светлый фон, тёмный текст, золото приглушено до читаемого на белом;
- шрифты крупнее по всей карточке;
- **три подписи под кружком вместо двух**: фамилия, должность, часы.
  Узнать человека по двум буквам в кружке нельзя — читают именно подписи.

Все цвета рисования идут через палитру `C`, жёстких значений в коде
отрисовки не осталось — проверено скриптом.

## 50. Под именами в печати рисовалось «undefined» (баг)

В приложении карточки собираются обходом по должностям, и поле `pos`
кладётся в каждую запись. В печати обход идёт **по часам начала смены** —
и при переписывании я перенёс `who`, `sh`, `k`, а `pos` забыл. Отрисовка
делала `String(c.pos).toLowerCase()` → строка «undefined» под каждым именем.

Должность кладётся в запись при группировке через `posName(q.pos)`.

Проверено скриптом: все поля, которые отрисовка читает у ячейки
(`who`, `sh`, `k`, `pos`, `kind`), действительно записываются при её создании.

## 51. Свернуть можно было только той же крошечной стрелкой

Открытие по всей площади починили, а закрытие осталось на стрелке в шапке.
И на скриншоте видно, почему она такая неприметная: стрелка стояла в конце
строки «старший: ФАМИЛИЯ ИМЯ», строка переносилась, и стрелка уезжала под
текст отдельным огрызком — целиться было не во что.

- Стрелка вынесена в **кружок фиксированного размера** справа в шапке,
  переноситься больше не может.
- Имя старшего в раскрытом виде из шапки убрано: оно уже подписано под его
  лицом, а в свёрнутом — осталось, там лиц не видно.
- Внизу раскрытой карточки добавлена строка **«свернуть ▴»** во всю ширину:
  пролистав сетку лиц, не нужно возвращаться к шапке.

Заодно исправлено `onClick={exportToday}` → `onClick={() => exportToday()}`:
после того как функция стала принимать формат первым аргументом, туда
приезжало **событие клика** вместо «chat» или «a4». Та же ошибка, что я уже
ловил с `generate` — проверил все обработчики, больше таких нет.

## 52. Дырка стояла не в своей группе по времени

Все недоборы валились в первую группу: если бармена не хватает в вечер,
кружок «нужен» всё равно стоял в 11:00 и показывал неправду. Я знал об
этом упрощении и предупреждал, но на готовой картинке оно читается как
ошибка данных.

Добавлен общий помощник `holesByHour(d)` — разносит недобор по сменам так же,
как его считает генератор: сначала по разбивке (сколько людей какой смены
ждали в этот день), остаток — на основную смену. Используется и в приложении,
и в печати, чтобы они не разошлись.

Проверено на четырёх сценариях:

```
✓ вечерняя дырка ушла в 17:00
✓ дневная дырка ушла в 11:00
✓ без разбивки обе дырки в основной смене
✓ остаток сверх разбивки — в основную
```

## 53. Печать: колонок столько, сколько людей

Сетка всегда была в четыре колонки. Смена из двух человек занимала половину
ряда, а одинокий кружок «нужен» — четверть, и правый край зиял пустотой.
Теперь колонок столько, сколько ячеек в группе, но не больше четырёх, и ряд
центрируется по своей ширине.

---

# Визуальная уборка по всему приложению — 20.09.2026

Не «добавить красоты», а убрать дрейф. В приложении 38 экранов, и каждый
собирали чуть по-своему: по отдельности всё смотрится нормально, а переход
между экранами спотыкается. Замеры до правки:

- **306 уникальных цветов** при ~25 описанных в `tokens.js`;
- **44 размера шрифта** — в диапазоне 9–15 px их десять, с шагом в полпикселя;
- **25 скруглений** при пяти в шкале `RADIUS` (её использовали 4 файла из 38);
- **17 значений отступа**, подряд от 1 до 20 — шкалы фактически не было.

## 54. Шкалы схлопнуты

| | было | стало |
|---|---:|---:|
| размеры шрифта | 44 | **12** |
| скругления | 25 | **8** |
| отступы | 17 | **9** |

Шкалы подобраны так, чтобы самые ходовые значения остались на месте, а
соседние подтянулись к ним: сдвиг ни для одного значения не больше 1 px.
Подтянуто 865 размеров шрифта, 176 скруглений, 281 отступ.
Капсулы (`borderRadius: 999`) не трогались.

## 55. Цвета-близнецы слиты

Из 306 цветов **116 оказались неразличимы глазом** — разница по каждому
каналу не больше шести единиц. Например `#EFE4C8`, `#F5E9CE`, `#E9DEC9`,
`#EAE0CC` и `#F5DFC8` — это один и тот же кремовый, записанный пятью
способами. При правке темы они разъехались бы в пять разных оттенков.

Слито 79 цветов в 37 эталонов (эталоном становился самый частый в группе):
**306 → 227**.

## Проверка

Главное здесь — доказать, что менялись **только значения**. Скрипт вырезает
из обеих версий все числа и hex-коды и сравнивает остаток символ в символ:
во всех 37 файлах остаток совпал. Единственное исключение — `schedule.jsx`,
который правился по делу всю неделю.

Баланс скобок сошёлся везде; четыре предупреждения проверялки
(`assistant`, `cocktail-editor`, `menu-trainer`, `search`) присутствуют
и в нетронутом исходнике — это её известные ложные срабатывания на
регулярных выражениях.

## 56. Главный экран: одна точка входа вместо четырёх карточек

Между человеком и уроком стояли три карточки **одного веса**: прогресс,
серия дней, настроение. У новичка все три пустые — 2 %, ноль дней, настроение
не отмечено, — и самым крупным пятном экрана оказывались семь пустых кружков
серии. Урок при этом начинался за сгибом.

- **Сверху герой-карточка** с разделом, на котором остановились: подпись
  «продолжить · тема · ≈ минут», крупный заголовок и золотая кнопка.
  Единственное яркое пятно на экране — промахнуться нельзя.
  Надпись меняется на «начать», если к разделу ещё не приступали.
- **Прогресс и серия — одной строкой**: «2 из 85 · полоска · ≈ 2 ч 51 мин».
  Это справка, а не действие, двух карточек они не стоят. Полоска на 2 %
  в отдельной карточке выглядела сломанной; теперь у неё минимум 2 % ширины,
  чтобы было видно, что она вообще есть.
- **Настроение и пульс команды — под список разделов.** Штука приятная, но
  не то, ради чего открывают приложение.

Стекло с изморозью разведено по обеим темам: в светлой блик белый и плотный,
в тёмной — тёплый янтарь.

### Поймано при проверке

- `fontFamily:serif` — привычка из `schedule.jsx`, но в `screens-learning.jsx`
  такой переменной нет, шрифт там пишут строкой. Вышла бы ошибка на рантайме.
- `StreakCard` остался в импорте мёртвым грузом после того, как карточка
  серии свернулась в строку — убран.

## 57. Главный экран роли: герой стал героем

Карточка «Твой трек» по смыслу уже была точкой входа, но по весу — нет:
тот же фон, та же рамка и то же скругление, что у «Графика смен» и «Книги
отзывов» под ней. Три одинаковые плашки подряд, глазу не за что зацепиться.

- Карточка получила **собственный фон** — светлее и золотистее соседних,
  скругление 18 вместо 14. «Морозный лёд» разведён по обеим темам.
- Заголовок 16 → 18, подпись 11 → 12 с увеличенным межстрочным.
- **Кнопка во всю ширину** вместо плашки в 9 px в углу. Прежняя по весу
  не отличалась от стрелки «›» у соседних карточек, то есть не читалась
  как действие вообще. Подпись из капса («ДАЛЬШЕ») переведена в обычный
  регистр — капс на кнопке шириной в экран выглядит криком.
- **«Пульс недели» переехал под героя.** Стоял над ним и первым делом
  сообщал статистику, хотя экран открывают ради действия, а не сводки.

### Проверка структуры

Правка визуальная, поэтому сверял не только баланс скобок, но и число
JSX-тегов до/после: в `screens-roleselect.jsx` все дельты совпали.
В `screens-learning.jsx` дельта `div` изменилась на единицу — оказалось,
это убранный самозакрывающийся `<div/>` заливки старой полоски прогресса,
заменённый на `<i/>`. Парные теги сошлись.

## 58. Четыре инструмента: сетка вместо тесной полосы

Справочник, SOS, Меню и Глоссарий стояли в ряд, сжатые в подписи по 9 px
с иконками 16 px, между карточкой «Книга отзывов» и заголовком «Треки
обучения». Главное — **SOS по весу был равен глоссарию**: срочная вещь и
словарь терминов выглядели одинаково.

- Сетка 2×2 с иконкой слева и подписью рядом: подпись 9 → 12 px, иконка 16 → 18.
- **SOS выделен**: своя красноватая заливка и рамка, полужирная подпись.
- Над группой появился заголовок «ПОД РУКОЙ» — раньше блок висел без
  объяснения между двумя другими.
- Люверс-украшение (`TokenEyelet`) убрано: в строчной плитке ему нет места.

## 59. Общий язык пустых состояний

В приложении **19 пустых состояний в восьми файлах**, и сделаны они вразнобой.
Крайние примеры: «Смена не найдена» — голый текст, читается как поломка;
«Здесь появится отзыв» — бумажная карточка с печатью, читается как обещание.
Один и тот же тип экрана вызывал противоположное чувство.

Добавлен компонент `EmptyState` в `widgets.jsx`: пунктирная рамка, иконка
в кружке, заголовок, пояснение и необязательная кнопка выхода. Пунктир тут
не украшение — он говорит «пока пусто», тогда как сплошная рамка выглядит
законченным блоком, в котором чего-то не хватает.

Применён к вкладке «Смена». Заодно исправлена формулировка: «Смена не
найдена» звучит как ошибка приложения, хотя это обычный выходной или ещё
не составленный график. Стало «Сегодня смен нет» с кнопкой «Открыть график».

### Что осталось

Остальные **18 пустых состояний не тронуты**. Они в файлах, которых я не
читал, и у каждого своя вёрстка — переделывать вслепую значит ломать.
Это работа на отдельный заход, по файлу за раз.

---

# Аудит перед сборкой — 20.09.2026

**1. Баланс скобок** — 38 файлов. Новых ошибок нет; четыре предупреждения
(`assistant`, `cocktail-editor`, `menu-trainer`, `search`) есть и в исходнике,
это известные ложные срабатывания проверялки на регулярных выражениях.

**2. Парность JSX-тегов** — во всех 38 файлах дельты `div`, `span`, `button`,
`svg`, `a` совпали с исходником.

**3. Охват массовой правки** — скрипт вырезает из обеих версий числа и hex-коды
и сравнивает остаток. Изменения сверх значений нашлись ровно в пяти файлах,
которые правились руками: `home-hubs`, `schedule`, `screens-learning`,
`screens-roleselect`, `widgets`. В остальных 33 менялись только значения.

**4. «Морозный лёд» — проверка обеих тем.** Скрипт ищет в новых блоках цвета,
не стоящие за проверкой `a11y`: такой цвет одинаков в обеих темах и в светлой
ломается. Нашлось два места в карточке трека:

- дорожка кольца прогресса `rgba(200,169,110,0.22)`;
- фон кружка иконки в «золотом» состоянии.

Оба существовали и раньше, но были незаметны: фон карточки был тёмным.
Сделав его светлым, я вытащил слабость наружу — на светлой теме дорожка
почти исчезла бы. Разведены по темам.

**5. Необъявленные имена** — проверены все идентификаторы из нового кода в
четырёх файлах. Все объявлены или импортированы. (Именно эта проверка вчера
поймала `fontFamily:serif`, которого в `screens-learning.jsx` не существует.)

**6. Мёртвый код** — после правок осиротели `StreakCard` (импорт остался
после сворачивания карточки серии в строку) и `TokenEyelet` (люверс жетона,
которому нет места в строчной плитке). Оба убраны.

**7. Регрессия генератора** — восемь утверждений из `tests/schedule-gen.test.js`,
серии смен, отпуска по месяцам: всё прежнее, дыр ноль.

## Чего аудит не покрывает

Без сборки не проверено, что JSX компилируется, и как всё выглядит вживую.
Сегодня изменились **все 38 экранов** — обязательно `npm run check:all`,
а потом глазами по вкладкам, включая светлую тему.

## 60. «Под рукой»: полка, которая не ломается при добавлении

Набор инструментов **динамический**: новичок видит три плитки, остальные
четыре, а завтра добавятся «Новички» и «Наставничество». Любая вёрстка,
заданная под текущее количество (полоса из четырёх, сетка 2×2, полка на три),
ломается при первом же добавлении: подписи режутся, лишнее уезжает за край.

Собрано так, чтобы количество не имело значения:

- **Чип по ширине своего слова**, полка переносит строки. Три помещаются
  в ряд, шесть уходят во второй — ничего не режется.
- **SOS вынесен из полки** отдельным кружком. Он не «где посмотреть»,
  а «что делать прямо сейчас»; в общем ряду читался таким же справочником,
  как глоссарий. Разная форма говорит об этом без слов.
- **Счётчик новинок меню — внутри чипа.** Снаружи он наполовину висел
  за краем плитки.

### Поймано при сборке

`saFrame(a11y, "soft")` — уровня «soft» эта функция не знает, она различает
только «full» и всё остальное. Молча вернула бы тот же цвет, что у полки,
и получилась бы рамка в рамке. Кромка чипа задана явно и мягче полки.

Проверено: число JSX-тегов совпало с исходником, все цвета полки разведены
по обеим темам.

## 61. Карточка трека: три способа сказать одно слились в один

Кнопка во всю ширину была молотком по гвоздю: карточка раздулась, съела воздух,
а роль при этом пропала с экрана — она пряталась только в заголовке.

Собрано заново:

- **Иконка роли с кольцом прогресса вокруг и значком типа шага.** Раньше это
  были три отдельные штуки: кружок с иконкой слева, кольцо с процентом справа
  и полоска снизу — три способа сказать одно и то же. Слились в узел 48×48,
  карточка сложилась в одну строку.
- **Тип шага значком**: ТЕСТ, ПРАКТИКА или УРОК. Человек заранее знает, на что
  подписывается — тест на восемь вопросов это другое обязательство, чем чтение.
  Приставка из названия убирается: «Тест: философия сервиса» → значок ТЕСТ
  и заголовок «Философия сервиса». Тип и название в одной строке спорили.
- **Подпись стала конкретной**: «8 вопросов · ≈6 мин · пройдено 25%» вместо
  «Следующий: „…" · ≈6 мин».
- **Роль вернулась чипом** над карточкой, с цветом роли и подписью вроде
  «Сезонный сотрудник».
- **Кнопка по размеру задачи** — не во всю ширину и не плашка в 9 px.
  Капс переведён в обычный регистр.

Проверено: число JSX-тегов совпало с исходником, все цвета разведены по
обеим темам, регрессия генератора чистая.

---

# Финальный аудит — 20.09.2026

**1. Баланс скобок** — 38 файлов, новых ошибок нет.

**2. Парность JSX** — `div`, `span`, `button`, `svg`, `a`, `circle`, `path`:
расхождений нет ни в одном из 38 файлов.

**3. Охват правок** — изменения сверх числовых значений ровно в пяти файлах,
которые правились руками: `home-hubs`, `schedule`, `screens-learning`,
`screens-roleselect`, `widgets`. В остальных 33 — только значения.

**4. «Морозный лёд»** — проверены все пять блоков, собранных сегодня:
герой новичка, карточка трека, полка «под рукой», `EmptyState`, пустое
состояние смены. **Неадаптивных цветов: 0.** Каждый цвет стоит за проверкой
темы либо входит в список заведомо одинаковых для обеих (тёмный текст на
золотой кнопке).

**5. Необъявленные имена** — проверены все идентификаторы нового кода
в четырёх файлах: объявлены или импортированы.

**6. Мёртвый код** — `StreakCard` и `TokenEyelet` вычищены ранее, повторных
хвостов нет.

**7. Регрессия** — восемь утверждений `tests/schedule-gen.test.js`, серии смен,
отпуска по месяцам, заполнение по должностям: всё прежнее, дыр ноль.

## Итог дня

Тронуто **38 экранов**. Механически: 865 размеров шрифта, 176 скруглений,
281 отступ, 79 цветов-близнецов. Руками: два главных экрана, полка
инструментов, общий компонент пустого состояния.

## Чего аудит не покрывает

Сборку и живой вид. Всё, что выше, — проверка текста кода, а не результата.
Обязательно `npm run check:all`, затем глазами по вкладкам **в обеих темах**.
Отдельно посмотреть: длинные названия уроков в карточке трека (ограничены
двумя строками с обрезкой) и полку «под рукой» при разном числе инструментов.

## 62. Инструменты: лента с прокруткой, SOS закреплён

Прошлые попытки ломались при добавлении кнопок: сетка 2×2 и полка на три —
это подгонка под сегодняшний состав, а набор динамический (новичок видит три,
остальные четыре, дальше добавятся «Новички» и «Наставничество»).

- **Лента прокручивается вбок.** Квадраты фиксированы в 62 px и не сжимаются,
  подписи не режутся. Высота ряда не растёт никогда, сколько бы ни добавили.
- **SOS вне прокрутки**, отделён вертикальной чертой. В панике он обязан быть
  на одном месте, а не уезжать вместе с лентой. Круг вместо квадрата —
  чтобы палец находил его, не читая подпись.
- **Свой цвет каждому инструменту** (те же оттенки, что у треков): однородный
  серый ряд глаз пропускает, цветной узор замечает боковым зрением.
- Заголовок «под рукой» убран — ряд отделён линиями сверху и снизу,
  подпись была лишней.
- Счётчик новинок меню — точкой в углу плашки: число на утилите это шум,
  важно что новое есть, а не сколько.

**Честно о цене.** Прокрутка — единственный вариант с постоянной высотой,
но четвёртый инструмент и дальше видны только если провести пальцем.
Владелец выбрал её, зная об этом.

## 63. Экран новичка приведён к тому же виду

Там оставалась первая версия с кнопкой во всю ширину, хотя карточку трека
мы с тех пор переделали дважды. Теперь оба главных экрана говорят на одном
языке: иконка с кольцом прогресса, значок типа шага (ТЕСТ / ПРАКТИКА / УРОК),
компактная строка и кнопка сбоку по размеру задачи.

Подпись собирается из того, что есть: «8 вопросов · ≈6 мин · пройдено 25%».
Кнопка меняется на «Начать», если к разделу ещё не приступали.

Проверено: парность JSX совпала с исходником, цвета разведены по обеим темам.

## 64. Три полосы подряд под лентой инструментов

Я обвёл ряд линиями сверху и снизу — и они пошли **от края до края экрана**,
тогда как всё остальное это карточки с полями. Полоса выглядела наклеенной
поверх вёрстки.

Но дело оказалось не только в ширине. **Сразу под блоком уже стоит
разделитель приложения** — линия с золотой звёздочкой, собственный приём
интерфейса. Мои две рамки добавились сверху, и получилось три горизонтальные
полосы подряд. Именно это владелец и увидел.

Итог: ряд убран в **капсулу, как у «Графика смен» и «Книги отзывов»** —
та же рецептура стекла, та же рамка, тот же отступ от края. Собственные
линии не нужны: карточка сама отделяет блок, и он перестаёт быть наклейкой
поверх вёрстки.

## 65. Капсула в капсуле: подпись едет сама

Итог долгого поиска. Проблема была в том, что подписи под иконками поднимали
блок выше соседних карточек, а рядом с иконками длинные слова переносились
на две строки.

Решение: **кружок с иконкой сидит внутри плашки, которая раздвигается и
впускает слово.** Активная подпись едет по очереди раз в четыре секунды.

Про анимацию по тапу, о которой спрашивал владелец, — от неё отговорил:
тап должен **открывать инструмент**, а не раскрывать его имя. Иначе каждое
обращение к справочнику стоит двух касаний ради подписи, которую человек
прочтёт один раз. Ротация по времени даёт то же знание бесплатно: за один
взгляд узнаёшь один инструмент, за полминуты — все, и тап остаётся тапом.

- Движение видно боковым зрением — это и решает исходную тревогу, что
  справочник с глоссарием будут пропускать.
- Четыре секунды на слово и плавный выезд: быстрее — получится вывеска.
- **Ротация уважает «спокойный режим»** системы (`prefers-reduced-motion`):
  кому движение мешает, тому подписи стоят на месте. Та же проверка, что
  в графике.

## 66. Пустой подбородок у капсулы инструментов

У SOS подпись стояла **под** кружком, а у инструментов — сбоку. Колонка SOS
выходила выше ряда, капсула тянулась по самой высокой — и под иконками
оставалась пустая полоса.

- Слово SOS переехало **внутрь круга**: обе стороны стали одной высоты.
  Надпись в круге работает как знак на огнетушителе — не нужно догадываться
  по иконке щита, что это такое.
- Скругление капсулы 18 → 999: теперь это и правда капсула, а не карточка.
- Содержимое выровнено по центру, отступы ужаты — тянуться больше нечему.

---

## 67. Следующий урок открывался снизу (баг)

Прокручиваемых контейнеров в приложении два — корень (`#root`) и тело
экрана (`T.lessBody`), — и **оба сохраняли позицию при смене содержимого**.
Из-за этого, дочитав длинный урок до конца и нажав «Дальше» внизу страницы,
человек попадал на следующий урок сразу в его конец: приходилось листать
вверх, чтобы начать читать.

Добавлен эффект в `App.jsx`: при смене экрана или урока прокрутка сбрасывается
в начало. Сбрасываются корень, `document.scrollingElement` и все внутренние
вертикальные скроллеры, у которых есть прокрутка.

**Горизонтальные ленты намеренно не трогаются** — условие
`scrollWidth <= clientWidth + 1` отсекает их. Там позиция осмысленна:
владелец отдельно отметил, что горизонтальный скролл работает как надо.

Эффект стоит внутри `ServiceAcademy` среди прочих хуков, до любых ранних
выходов — порядок хуков не нарушен.

## 68. Цвет кнопки и цвет экрана разъезжались

Иконка «Глоссария» обещала сиреневый, а экран открывался золотым. Причина
простая: цвет иконки задавался в `screens-roleselect.jsx`, а цвет экрана —
жёстким `color="#C8A96E"` в `App.jsx`. Два места, два значения.

Добавлен **единый источник** в `ui/tokens.js`:

```js
export const TOOL_COLOR = { sp: …, menu: …, gl: … };
export const toolColor = (key, a11y) => …
```

Иконка в ленте и экран глоссария берут цвет оттуда. Обе темы учтены:
у каждого инструмента свой оттенок для тёмной и для светлой.

### Что осталось несогласованным

`GlossaryScreen` **уже принимал цвет параметром** — поэтому починился одной
строкой. А `MenuTrainerScreen` и `ReferenceSection` такого параметра не имеют,
их палитра зашита внутри. Чтобы и они подхватывали цвет кнопки, нужно сначала
провести `color` внутрь этих экранов — это отдельная правка по их коду,
вслепую делать не стоит.

Пока этого нет, честнее держать иконки «Меню» и «Справочника» в тех цветах,
какие у этих экранов на самом деле. Иначе кнопка снова будет обещать одно,
а открывать другое.

## 69. Цвет входа продолжается внутри — как у SOS

Владелец показал эталон: у SOS иконка красная и экран внутри красный,
переход не сбивает с толку. Того же хотелось от «Меню» и «Справочника».

Оба экрана оказались к этому готовы: акцент у них сведён **в одну переменную
`gold`**, а не размазан по файлу. Достаточно было принять цвет параметром:

```js
const gold = accent || (a11y ? "#8B6A30" : GOLD);
```

`App.jsx` передаёт им цвет из того же `toolColor()`, что красит иконку в ленте.
Теперь вся тройка согласована: сиреневый глоссарий, зелёное меню, золотой
справочник — и кнопка, и экран.

Значение по умолчанию оставлено прежним: если в эти экраны заходят другим
путём (не из ленты), вид не меняется.

## 70. Фон графика отличался от всего приложения

График рисовал **собственный плоский фон** `#1A1008`, тогда как всё
приложение — тёплый градиент с золотым свечением сверху (`ui/styles.js → app`).
На переходе с главной подмена была заметна: экран выглядел так, будто он
из другого приложения.

Фон приведён к общему: та же радиальная подсветка и тот же градиент, отдельно
для тёмной и светлой темы. Плоская заливка убрана — в светлой теме она тоже
была одноцветной (`#E8DEC8`), тогда как остальные экраны там имеют переход.

---

# Обучение и гид сверены с тем, что построено — 20.09.2026

## 71. Первый вход: две карточки описывали старое приложение

Три карточки «первого знакомства» (`sa_welcome_seen`) показываются один раз
в жизни — и две из них рассказывали о том, чего уже нет.

- **«График смен — в кармане»** обещал «кнопки „попросить выходной"
  и „не смогу выйти"» внутри дня и «имя в капсуле с трубкой». И то и другое
  мы заменили: пожелания отмечаются сеткой месяца (в том числе периодом
  за два касания), состав смены показывается лицами.
- **«График: режим редактора»** не знал ни про палитру смен, ни про отмену,
  ни про заполнение по должностям, ни про автосборку — то есть про всё,
  что мы добавили на этой неделе.

Обе переписаны.

## 72. Гид: две темы врали, одной не было вовсе

- **«График смен»** описывал прежний способ отмечать выходные — тот самый,
  ради отказа от которого всё и затевалось.
- **«Звонок в один тап»** описывал капсулы с трубкой и одну картинку;
  теперь это лица и два формата печати.
- **Добавлена тема «Редактору: как собрать месяц»** — её не было. Человек,
  который собирает график, не узнал бы из гида ни про закрепление вручную
  поставленной смены, ни про «↩», ни про заполнение по должностям,
  ни про автосборку.

## Что осталось непроверенным

Я сверял только темы про график и смену — те, что мы меняли. В гиде больше
сорока тем, и остальные (тренажёры, колоды, рейтинг, аналитика) я не читал:
они могли устареть от прежних правок, но это вне того, что мы делали.

---

# Контекстные подсказки вместо лекции при первом входе — 20.09.2026

## 73. Первый вход был лекцией на двадцать карточек

Посчитано: новичок листает **16 карточек, менеджер 19, владелец 20**.
В комментарии к самому коду написано, что задача первого входа — «снять
тревогу „куда я попал", а не обучить: детали человек узнает по контекстным
подсказкам». Сделано было ровно наоборот: до первого экрана пересказывался
весь гид. (Там же обнаружились **два разных описания порядка карточек
подряд** — логику переписывали, комментарий остался от прежней версии.)

Решение владельца, и оно вернее моего: оставить четыре карточки на
ориентацию, а остальное отдать подсказкам в месте и в момент, когда вопрос
уже возник.

## 74. Механизм подсказок

В `widgets.jsx` добавлены `useHintOnce(key, ready)` и `HintBubble`.

- **Переиспользована существующая механика.** Пульсация — класс `.sa-pulse`,
  тот же, что в уроках; он **уже погашен** в системном «спокойном режиме»
  (`prefers-reduced-motion`), так что ничего добавлять не пришлось.
  Флаг «видел» — по образцу `sa_hint_mistakes`, единственной подсказки,
  которая в приложении уже была.
- **Стрелка указывает на кнопку**, о которой речь, и эта кнопка пульсирует.
- **Роли разведены флагами**: `sa_hint_sched_staff` против `…_admin`.
  Без этого менеджер, начинавший стажёром, своей подсказки не увидел бы
  вовсе — отметка «видел» была бы уже поставлена.
- Подсказка ждёт `state === "ok"`: на пустом или ещё не загруженном экране
  объяснять нечего.
- Два выхода: «Дальше» по шагам и «больше не показывать» сразу.

## 75. График: подсказки для обеих ролей

**Сотруднику** два шага: где ближайшая смена и что тап по дню покажет состав;
затем — как отмечать выходные и отпуск периодом. На втором шаге пульсирует
карточка «Мои пожелания».

**Руководителю** три: «Заполнить черновик» и заполнение по должностям
(пульсирует сама кнопка); палитра по тапу и закрепление вручную поставленной
смены; отмена и то, что до «Сохранить» всё остаётся черновиком.

## Что дальше

Это образец на одном разделе. Если ритм верный — тем же механизмом
размножается на меню, колоду, книгу отзывов, команду. Тексты для каждого
раздела в двух ролях — отдельная работа, и делать её лучше не подряд.

## 76. Подсказка на главном экране

Второй раздел с контекстными подсказками, тем же механизмом.

- **Сотруднику** три шага: карточка следующего шага и кнопка в него;
  лента инструментов и SOS; четыре вкладки внизу.
- **Руководителю** те же три, но про своё: карточка обучения и график смен
  со сборкой месяца; та же лента; вкладка «Команда» с рейтингом, новичками
  и аналитикой.

Второй шаг подсвечивает **ленту инструментов** — ту самую, про которую
владелец беспокоился, что справочник с глоссарием будут пропускать.
Подсказка называет их прямо и объясняет, почему подписи меняются по очереди.

Ключи снова разведены по ролям (`home_staff` / `home_admin`): роль здесь
определяется должностью в профиле, а не выбранным треком.

## 77. Подсказка пересобрана по вкусу владельца

Первая версия была собрана по механике, а не по языку приложения: плоская
заливка вместо морозного стекла и стрелка, склеенная из CSS-границ.

Стало: **компоновка в одну строку** (иконка, текст с полосками шагов, кнопка)
и **обвязка без пунктира** — закрытие крестиком в углу вместо слова «скрыть».
Крестик понятнее: жест привычен и не требует слов.

Шаги показаны полосками, а не цифрами «1 из 3»: видно, сколько осталось,
не читая. Активная полоска длиннее и плавно переезжает.

**Про ромбик-стрелку.** Владелец отдельно отметил, что он некрасив, и был
прав: у ромба были собственные грани, и на стыке с пузырём проступала лишняя
линия — он выглядел наклейкой, а не продолжением стекла. Границы убраны,
заливка взята та же, ромб сдвинут под край пузыря и перекрыт им.

## 78. Подсказки во вкладке «Смена»

Третий раздел. `HubScreen` общий для «Смены» и «Команды», поэтому ключ и
тексты передаются **снаружи** (`hintKey`, `hintSteps`): объяснять этим
вкладкам надо разное, и по-разному сотруднику и руководителю.

- **Сотруднику**: карточка сегодняшней смены и тап по лицу коллеги ради
  номера; ниже чек-листы и задание дня.
- **Руководителю**: та же карточка, но про то, кого не хватает; чек-листы
  открытия и закрытия, которые видит старший.

Механизм тот же: один раз, крестик в углу, ключ свой на роль
(`shift_staff` / `shift_admin`).

Заготовка готова и для «Команды» — достаточно передать `hintKey` и
`hintSteps` в тот же `HubScreen`.

## 79. Подсказки во всех четырёх вкладках

Закрыты «Команда» и «Я» — механизм уже стоял, понадобились только тексты.

**«Команда»**
- Руководителю: команда целиком — кто растёт, кто застрял, кто давно
  не заходил; «Новички» и «Аналитика» (где проседает тема, а не человек).
- Сотруднику: рейтинг считается по тестам, а не по стажу — новичок обгоняет
  опытного, если учится; рекорды команды пополняются автоматически.

**«Я»** — одна на всех: прогресс, сертификаты, смена роли. Второй шаг
объясняет главное про сам механизм: подсказки показываются **один раз**,
а гид открыт всегда. Без этого человек, закрывший подсказку, не знал бы,
куда вернуться за тем же текстом.

Итого подсказки есть на: главном экране, графике, «Смене», «Команде» и «Я» —
в двух ролях везде, кроме «Я», где разницы нет.

Ключи: `home_*`, `sched_*`, `shift_*`, `team_*`, `me`.

## 80. Тексты подсказок вынесены в один файл

Предложение владельца, и оно верное не только по удобству. Тексты были
вписаны прямо в вёрстку четырёх файлов — править их страшно, потому что
рядом код, и любая опечатка ломает экран.

Создан `data/hints.js`: только слова, сгруппированы по разделам и ролям,
с комментарием, как править. Компоненты берут их через `hintsFor(ключ)`,
а ключ с учётом роли собирает `hintKey(база, isAdmin)`.

В файле записаны и правила, по которым тексты писались:

- шаг — одно предложение, максимум два;
- шагов на раздел два-три, четыре уже утомляют;
- говорить то, чего человек **не увидит сам** — правило, следствие, причину.
  «Рейтинг считается по тестам, а не по стажу» работает;
  «Тап по кнопке откроет рейтинг» бесполезен, это и так видно.

Проверено: в компонентах текстов подсказок не осталось.

## 81. Подсказки в справочнике, меню и глоссарии

Те самые три экрана, из-за которых всё началось: владелец опасался, что
справочник с глоссарием будут пропускать.

- **Справочник** и **глоссарий** — по одной подсказке на всех: содержание
  от роли не зависит. Второй шаг в обоих рассказывает про звёздочку
  и заметки — про них иначе не узнать, а они превращают чужой текст в свой.
- **Меню** разведено по ролям: сотруднику про то, что точка на иконке значит
  новые блюда и смотреть их надо **до** смены; руководителю про то, что
  правка и стоп-лист доходят до команды в тот же день.

Все тексты — в `data/hints.js`, рядом с остальными.

Подсказок теперь восемь разделов: главный экран, график, «Смена», «Команда»,
«Я», справочник, меню, глоссарий.

## 82. Подсказки в SOS и у наставника

**SOS — один шаг, и это намеренно.** Сюда заходят в панике, читать инструкцию
некогда. Поэтому сказано только то, что делать прямо сейчас: выбрать ситуацию
и взять первую строку, не дочитывая до конца.

**Наставник** — два шага. Первый про то, как спрашивать (живым языком, а не
командами) и что он знает стандарты именно вашего ресторана. Второй — про то,
чего человек сам не узнает и без чего не станет спрашивать всерьёз:
**наставник не оценивает и не докладывает руководству**. Можно спросить то,
что неловко спрашивать у старшего в разгар смены.

Подсказок теперь в десяти разделах.

## 83. Сборка падала на подсказке у наставника (моя ошибка)

`ui/assistant.jsx:490` — «Expected `}` but found `&&`». Я вставил подсказку
сразу после `{msgs.length === 0 && !confirmClear && (`, а там ожидается
**JSX-элемент**, а не выражение в фигурных скобках. Подсказка перенесена
первым ребёнком карточки внутрь `<div className="sa-pagein">`.

### Почему проверка промолчала

Скобки при такой ошибке **сбалансированы** — мой контроль баланса и парности
тегов её увидеть не мог в принципе. Это дыра в методе, а не случайность.

Добавлен `jsxlint.py`: ищет строки, где после `&& (`, `? (`, `=> (` или `: (`
следующая строка начинается с `{`. Прогнан по всем 38 файлам и `App.jsx` —
других таких мест нет.

Вывод на будущее: баланс скобок доказывает только то, что ничего не оборвано.
Грамматику JSX он не проверяет, и без настоящей сборки такие ошибки ловятся
только точечными правилами вроде этого.

## 84. Работа над ошибками, книга отзывов, задание дня

**«Работа над ошибками»** — самая неочевидная механика приложения: слово
«ошибки» звучит как наказание, и ею просто не пользуются. Подсказка снимает
это прямо: сюда попадают неверные ответы **не чтобы пристыдить**, а чтобы
вернуться, пока не отложилось. Второй шаг объясняет интервалы — день, три,
неделя, — про которые иначе не догадаться.

**Книга отзывов** — что отзывы настоящие, от гостей, и что имя появляется,
когда отметили именно твою работу. Печати внизу — общий след месяца.

**Задание дня** — что серия считается по дням, а не по количеству: закрыл
пять, день засчитан. И что пропуск обнуляет серию, но не прогресс.

Подсказок в тринадцати разделах. Новый `jsxlint.py` прогнан по всей кодовой
базе после каждой вставки — чисто.

## 85. Тексты для всех оставшихся общих разделов

Написаны и лежат в `data/hints.js`: колода коктейлей, бар-лаб, рейтинг,
статистика, профиль, сертификаты, гость недели. Всего в файле 25 ключей.

Подключён **рейтинг** — там объяснено главное, чего с экрана не видно:
место считается по среднему баллу за последние недели, а не по стажу,
поэтому новичок может быть первым.

**Коктейли пропущены намеренно.** У `CocktailsScreen` другая структура
корня — шаблонная вставка туда не подошла, а подгонять вслепую значит
рисковать. Текст готов, подключение — точечная правка по месту.

Три текста стоит отметить отдельно, они говорят то, чего иначе не узнать:

- **профиль**: имя и фамилия должны совпадать с графиком — по ним
  приложение находит смены. Несовпадение выглядит как «график не работает»;
- **профиль**: телефон видят коллеги по смене только при открытии карточки
  дня, руководство — всегда. Это про доверие, и умалчивать о нём нельзя;
- **гость недели**: это не тест, правильных ответов несколько. Без этого
  человек боится ошибиться и отвечает шаблонно.

---

# Версионирование подсказок и финальный аудит — 20.09.2026

## 86. Правка текста доходит до тех, кто подсказку уже закрыл

Владелец попросил включить подсказки заново. Сделано **не разовым сбросом,
а версией** — иначе при каждой правке текста пришлось бы чистить вручную,
и исправленную формулировку увидели бы только новые сотрудники.

- `HINTS_VERSION` в `data/hints.js`. Ключ хранения стал
  `sa_hint_v{версия}_{раздел}`: поднял число — подсказки показались заново
  всем и по одному разу.
- У карточек первого входа **своя версия уже была** (`sa_welcome_seen_v9`).
  Я чуть не завёл вторую рядом — вместо этого поднял существующую до `v10`.

## Аудит

**Баланс скобок** — 39 файлов, новых ошибок нет.
**Грамматика JSX** (`jsxlint.py`, написан после вчерашнего падения сборки) —
чисто во всех файлах.
**Парность тегов** с учётом самозакрывающихся — расхождений нет.
**«Морозный лёд»** — проверены все блоки, собранные сегодня: `HintBubble`,
`EmptyState`, капсула инструментов, карточка трека, герой новичка.
**Неадаптивных цветов: 0.**

**Механика подсказок** — во всех десяти экранах импорты и отрисовка на месте.
(`home-hubs.jsx` не импортирует `hintsFor` намеренно: тексты приходят туда
параметрами, потому что `HubScreen` общий для двух вкладок.)

**Ключей в `data/hints.js` — 25, подключено 19.** Готовы, но ещё не
подключены шесть: `barlab`, `certificates`, `cocktails`, `profile`, `stats`,
`weeklyGuest`. Тексты написаны, нужна точечная вставка по месту.

**Регрессия генератора** — восемь утверждений, серии смен, отпуска: чисто.

## 87. Подсказка в глоссарии сжималась в узкую колонку (моя ошибка)

Пузырь попал **внутрь шапки** `lessHead`. Шапка — флекс-РЯД, и пузырь стал
её элементом наравне с заголовком: ужался до трети ширины, текст пошёл
по одному-два слова в строку.

Причина в том, как я вставлял: искал первый `</div>` после открытия шапки —
а он закрывал вложенный заголовок, не её саму. Со второй попытки повторил
ту же ошибку. Помогло только определение конца блока **подсчётом вложенности**,
а не поиском первого закрывающего тега.

### Третья проверка

Добавлен `flexhint.py`: ищет `<HintBubble`, у которого ближайший родитель —
флекс-ряд. Колонки исключены намеренно: в них пузырь растягивается на всю
ширину и это правильно, опасен именно ряд.

Прогнан по всем файлам — других таких мест нет.

Теперь проверок три, и каждая появилась после реальной поломки:
`check.py` — баланс скобок, `jsxlint.py` — грамматика JSX после падения
сборки, `flexhint.py` — вёрстка пузыря после этого случая.

## 88. «Can't find variable: rHint» — подсказка в чужом компоненте

В `ReferenceSection.jsx` **несколько компонентов в одном файле**. Состояние
я объявил в `ReferenceSection` (строка 217), а пузырь вставил после
`<div ref={bodyRef}>` — а он принадлежит компоненту `Lesson` (строка 151),
объявленному ВЫШЕ. Переменной там просто нет, и экран падал.

Починено передачей готового узла: `Hub` принимает `hintNode` параметром,
состояние остаётся в `ReferenceSection`. Тот же приём, что с `HubScreen`.

### Четвёртая проверка

`scopecheck.py`: сверяет, что переменные подсказок используются в том же
компоненте, где объявлены. Ошибка этого класса не видна ни балансу скобок,
ни грамматике JSX — только в браузере.

Первая версия проверки дала одиннадцать ложных срабатываний: шаблон ждал
пару `[x, setX]`, а `useHintOnce` возвращает `[rHint, rHintDone]`. Исправлено,
после чего — чисто во всех файлах.

## Итог: четыре проверки, четыре поломки

| проверка | появилась после |
|---|---|
| `check.py` | баланс скобок, с самого начала |
| `jsxlint.py` | падение сборки на наставнике |
| `flexhint.py` | сжатый пузырь в глоссарии |
| `scopecheck.py` | «Can't find variable» в справочнике |

Каждая написана **после** реальной поломки, и каждую нашёл владелец, а не я.
Это и есть главный вывод дня: проверки закрывают то, что уже ломалось,
и ничего не говорят о том, что сломается завтра.

## 89. Дубликаты в импорте — сборка снова упала

`screens-gamification.jsx:23`: «The symbol useHintOnce has already been
declared». Я подключал к этому файлу **два экрана** — рейтинг и задание дня,
— и скрипт каждый раз дописывал `useHintOnce, HintBubble` в один и тот же
импорт. Вышло `…, useHintOnce, HintBubble, useHintOnce, HintBubble`.

### Пятая проверка

`dupimport.py`: ищет повторяющиеся имена внутри одного `import { … }`.
Заодно прогнан по всем файлам — дубль был ровно один, исправлен.

## Пять проверок, пять поломок

| проверка | появилась после |
|---|---|
| `check.py` | баланс скобок, изначально |
| `jsxlint.py` | падение сборки на наставнике |
| `flexhint.py` | сжатый пузырь в глоссарии |
| `scopecheck.py` | «Can't find variable» в справочнике |
| `dupimport.py` | дубликат импорта в геймификации |

Все четыре последние написаны **после** поломки, и каждую нашёл владелец.
Закономерность в том, что скриптовые правки по шаблону ломаются в местах,
где файл устроен не так, как я предполагал: другой компонент, флекс-ряд,
второй вызов в том же файле. Проверка на баланс скобок ни одну из них
увидеть не могла.

---

## 90. Карточки первого входа наконец сокращены

Я обещал урезать их до четырёх и **не сделал** — мы ушли в контекстные
подсказки, и я к этому не вернулся. Владелец заметил: карточек по-прежнему
шестнадцать у новичка, девятнадцать у менеджера, двадцать у владельца.

Осталось четыре, на ориентацию:

1. четыре вкладки — карта приложения;
2. твой трек — как устроено обучение;
3. наставник — то, чего сам не найдёшь;
4. гид всегда под рукой — где искать ответы дальше.

Карточки руководителя убраны тоже: редактор графика и меню объясняются
подсказкой **при первом входе туда**, а не заранее и не всем подряд.

Версия ключа поднята до `v11`, чтобы новый набор показался всем.

## 91. Подсказку было легко пропустить

На записи экрана видно: пузырь читается как часть интерфейса, глаз по нему
скользит. А он приходит один раз и должен сработать.

- **Надпись «ПОДСКАЗКА»** над текстом. Без неё непонятно, что это такое
  и почему оно здесь.
- **Мягкое золотое дыхание первые три цикла** (`.sa-hintglow`): свечение
  расходится и гаснет, глаз ловит движение боковым зрением. Потом пузырь
  замирает — это не мигание и не раздражает.
- Анимация добавлена в тот же список, что гасится в системном
  «спокойном режиме».

## 92. Подсказка меньше и указывает на цель

**Меньше:** отступы 13→10, иконка 30→24, текст 13.5→12.5, кнопка ужата,
полоски шагов тоньше. Пузырь занимает примерно на треть меньше экрана.

**Указывает:** добавлен параметр `at` — хвостик сдвигается влево, вправо
или остаётся по центру.

Про настоящую привязку к элементу скажу прямо: она потребовала бы измерять
положение цели на экране, позиционировать пузырь абсолютно и пересчитывать
при прокрутке и повороте, плюс обрабатывать случаи, когда цель уехала за
край. Это отдельная работа и новый источник поломок.

Здесь сделано дёшево и почти с тем же эффектом: пузырь и так стоит вплотную
к тому, что объясняет, и цель пульсирует. Не хватало только направления —
по центру хвостик указывал в никуда.

Направление задано там, где цель не по центру: «Заполнить черновик» в графике
и лента инструментов на главном — обе слева.

## 93. Настоящая привязка подсказки к элементу

Возражение владельца: «поломку можно починить, а неудобство…». Он прав —
сломанная сборка видна сразу и чинится за час, а плохое решение живёт годами
и его никто не замечает. Сделано по-настоящему.

- **`anchorRef`** — ссылка на объясняемый элемент. Положение измеряется
  и пересчитывается при прокрутке, повороте и смене шага.
- **Подсветка цели**: всё вокруг притемняется, цель обведена золотом.
  Вопрос «что именно мне объясняют» больше не возникает. Дыра сделана
  огромной тенью вокруг рамки — без SVG-масок, работает везде.
- **Пузырь идёт за целью**: встаёт под ней, а если места нет — над.
  По горизонтали прижимается к краям, чтобы не уезжать на узких телефонах.
  Хвостик указывает точно в центр цели.
- **Цель подводится в кадр** (`scrollIntoView`): иначе подсветка оказалась бы
  там, куда человек не смотрит.
- Без `anchorRef` компонент работает как раньше — обычный пузырь в потоке.
  Это для разделов, где объясняется экран целиком, а не отдельная кнопка.

В графике привязаны все три шага руководителя: «Заполнить черновик»,
таблица смен, «Сохранить». Остальные разделы пока по-старому — там
объяснялся экран в целом.

## 94. «Показать подсказки заново» — в гиде

Владелец спросил, почему подсказок нет: он их уже посмотрел, отметка стоит.
Поднимать версию каждый раз — плохой ответ: это показывает их **всем сразу**,
включая тех, кто уже разобрался.

Добавлена кнопка в конце гида. Нажал — подсказки вернутся у тебя одного,
и встретят в тех же разделах, где и в первый раз.

Это нужно не только для проверки. Человек закрыл подсказку, а через месяц
забыл, как устроен раздел, — и раньше вернуть её было нельзя никак.

Версия поднята до `3`, чтобы текущие правки дошли и до тех, кто видел
прошлые.

## 95. Анимации подсказки сглажены

- **Подсветка и пузырь двигаются по одной кривой** (`cubic-bezier(.22,1,.36,1)`,
  0.5 с) — раньше рамка ехала за 0.25 с, а пузырь прыгал мгновенно, и при
  смене шага они шли вразнобой.
- **Пузырь переезжает, а не появляется заново**: добавлен переход по
  положению, поэтому между шагами он скользит к новой цели.
- **Затемнение проявляется плавно** вместо мгновенного.
- **Вход мягче**: подъём 16 → 9 px плюс лёгкий масштаб, длительность
  0.38 → 0.55 с. Рывок заменён на всплытие.
- **Дыхание спокойнее**: 2.1 → 2.8 с и более пологая кривая.

Всё по-прежнему гаснет в системном «спокойном режиме».

## 96. Кнопку возврата подсказок было не найти

Я поставил её в конец гида — а в гиде больше сорока тем. Чтобы до неё
добраться, нужно пролистать весь экран. Та же ошибка, что с помощником
по норме в настройках: кнопка относится ко всему разделу, а лежала под ним.

Перенесена под шапку гида и сделана компактнее. Теперь она на первом экране.

Версия поднята до `4`. Дальше поднимать не придётся — для возврата подсказок
есть кнопка, и она наконец на виду.

## 97. «Летающий» пузырь

Я добавил плавный переход по положению — и пузырь начал **лететь через весь
экран** к новой цели. На большом расстоянии плавное движение читается не как
переход, а как посторонний объект, пролетающий мимо.

Исправлено разделением ролей:

- **подсветке скользить уместно** — она связывает старую цель с новой,
  взгляд идёт за ней;
- **пузырю — нет**. Переход по положению убран, вместо него `key` по шагу:
  пузырь гаснет и всплывает уже на месте.

Всплытие заодно укорочено: 0.55 → 0.34 с, подъём 9 → 7 px. Появляться
на месте долго незачем, длинная анимация здесь только тормозит.

## 98. Подсветка прыгала, прежде чем встать на место

Две мои правки столкнулись друг с другом. Цель подводилась в кадр **плавной
прокруткой**, а у рамки подсветки стоял плавный переход по положению. Во время
прокрутки координаты меняются каждый кадр, переход гонится за ними и не
успевает — рамка дёргается, пока всё не остановится.

Прокрутка сделана мгновенной. Гонка снимается в корне: координаты измеряются
один раз, когда всё уже на местах. Контрольный замер — через
`requestAnimationFrame`, после того как браузер перерисовал экран.

Плавность при этом не потеряна: рамка по-прежнему скользит между шагами,
потому что там прокрутки нет и гнаться не за чем.

## 99. Хвостик подсказки и подсветка в светлой теме

**Хвостик** был повёрнутым квадратом. Его грани идут по всем четырём
сторонам, и на стыке с пузырём проступал контур — на скриншоте видно
«шеврон», висящий отдельно от подсказки.

Заменён на настоящий SVG-треугольник: заливка одна с пузырём, обводка только
по двум наклонным граням, а основание перекрыто полосой того же цвета —
шва не остаётся. Переделаны оба хвостика, привязанный и обычный, иначе
в разных разделах подсказка выглядела бы по-разному.

**Светлая тема**: затемнение `rgba(40,30,10,0.42)` на светлом фоне почти
не читалось. Поднято до `0.58`, рамка цели с 1.5 до 2 px и в более тёмном
золоте, свечение вокруг усилено. В тёмной теме всё как было — там и так
хватало.

## 100. Рывок при первом показе подсветки

Между шагами всё двигалось плавно, а первое появление рамки было рваным.

Причина: при первом показе замер идёт **дважды** — сразу и после перерисовки
браузером. Между ними бывает разница в пару пикселей, и переход честно её
анимировал: получался короткий рывок уже после того, как рамка встала.

Переход теперь включается только после того, как рамка встала на место
(`ready`). Первая установка мгновенная, все последующие — плавные.
Между шагами замер один, поэтому там ничего не изменилось.

## 101. Страничные подсказки терялись на светлой теме

У привязанной подсказки контраст создаёт затемнение вокруг цели. У страничной
его нет — и на светлой теме кремовый пузырь лежал на кремовом фоне почти
незаметно.

Для страничной на светлой теме: фон плотнее, кромка заметнее, добавлена тень,
чтобы она отделялась от страницы. В тёмной теме всё как было — там контраста
хватало.

Признак «страничная» берётся по наличию параметра `anchorRef`, а не по его
содержимому: на первом кадре ссылка ещё пуста, и привязанная подсказка на миг
получила бы чужой вид.

### Что это говорит о моих проверках

«Морозный лёд» проверяет, стоит ли цвет за условием темы. Здесь **стоял** —
и всё равно было плохо, потому что контраст цвета с фоном проверка не мерит.
Третий раз за день светлую тему ловит владелец, а не скрипт.

## 102. Та же ошибка, что вчера, — и проверка её опять не увидела

`Can't find variable: hintNode`. Я вставлял `{hintNode}` после `<Head …/>`
в `ReferenceSection.jsx` — и поиск нашёл `<Head` в **следующем** компоненте
файла (`Course`), а не в `Hub`, куда параметр передаётся. Ровно тот же
промах, что вчера с `rHint`.

Перенесено в `Hub`, сразу после открытия его корневого блока.

### Почему `scopecheck.py` промолчал

Её шаблон искал имена вида `rHint`, `gbSteps` — «до трёх букв плюс Hint».
`hintNode` под него не подходит. Проверка была написана под конкретный
вчерашний случай, а не под класс ошибок.

Расширена до любого имени со словом hint. При этом пришлось научить её
отличать настоящие обращения от похожего текста: имена JSX-атрибутов
(`hintNode={…}`), ключи объектов (`hint: …`) и строки с классами
(`"sa-hintin"`) — не обращения к переменным, и первая широкая версия дала
восемь ложных срабатываний.

Урок тот же, но резче: **проверка, написанная под один случай, ловит только
его**. Вчера я закрыл `rHint`, сегодня тем же способом сломал `hintNode`.

## 103. Подсветка на главном экране

Привязаны обе цели: карточка трека (шаг 1) и лента инструментов (шаг 2).

При этом чуть не повторил свежую ошибку: объявил `refTrack`, передал его
в `anchorRef` — и забыл прицепить к самой карточке. Код при этом рабочий,
ошибки нет: подсказка просто тихо теряет подсветку и ведёт себя как
страничная. Заметить это можно только глазами.

### Шестая проверка

`refcheck.py`: ссылка объявлена или передана в `anchorRef`, но ни к одному
элементу не привязана. Единственная из шести, которая ловит не поломку,
а **молчаливую потерю функции** — остальные пять реагируют на то, что уже
сломалось.

## 104. Третий шаг указывает на навбар

Он рассказывает про четыре вкладки внизу, а подсвечивал ленту инструментов —
то есть указывал в пустоту.

Навбар живёт в `tabbar.jsx`, а объясняет его экран «Учусь». Тянуть ссылку
через три компонента ради одной подсветки дороже, чем найти элемент по имени:
`HintBubble` научился принимать `anchorId` и искать цель в документе.

Это пригодится и дальше — общих элементов вроде навбара в приложении хватает.

---

# Разговор 21.09 (продолжение) — правки 105–112

## 105. Проверки переписаны на настоящем разборе кода

Шесть проверок из прошлого разговора жили в `/home/claude/` и пропали вместе
с ним. Написаны заново — но не регулярными выражениями, а на настоящем
разборе JSX (TypeScript) и настоящем сборщике (esbuild, полная сборка от
`main.jsx`). Лежат теперь в проекте: `tools/claude/sa-check.mjs`, чтобы
следующему разговору не писать их в третий раз.

Ложных срабатываний по исходнику — ноль (у старых скриптов «нормой» были
4 и 2). Проверка каждой проверки: нарочно испорченная копия — все ловят.

## 106. «Рейтинг» падал при первом входе

`<HintBubble a11y={a11y}>`, а переменной `a11y` в `LeaderboardScreen` нет —
экран всюду берёт `T.a11y`. При первом входе подсказка ещё не видена,
пузырь рисуется — и весь экран уходит в «Что-то пошло не так».

Хуже того, падение повторялось **при каждом входе**: подсказка ни разу не
успевала закрыться, отметка «видел» не ставилась. Раздел был недоступен
всем, у кого подсказка v4 не отмечена, — то есть всем.

Нашла это проверка областей видимости (tsc) — та же, что давно лежит в
`tests/scope-check.sh`. `scopecheck.py` из прошлого разговора её не видел:
он искал только имена подсказок (`rHint`, `hintNode`), а сломалось чужое
имя. Это ровно урок из правки 102: проверка под один случай ловит только
его. Проверка под класс ошибок («любое необъявленное имя») поймала сразу.
Подтверждено в браузере до правки и после.

## 107. Подсказка «Заданий дня» не показывалась никогда

В `DailyScreen` два `return`: ранний — «Сначала выбери роль» — и основной.
Пузырь был вставлен только в ранний: поиск нашёл первый `<div style={T.screen}>`
в файле. В обычной работе подсказки не было вовсе, а в ранней ветке она
падала (та же `a11y`, что в правке 106).

Перенесена в основную ветку, под шапку. Заодно выяснилось, что текст был про
другое: «пять задач», «серия», «закрыл пять — день засчитан» — это «пятёрка
дня» из колод меню и бара (`bumpDaily` в `menu-deck.jsx` и `cocktails.jsx`).
На этом экране три урока и глава справочника, серии нет. Текст переписан по
коду: уроки — в первую очередь непройденные; глава — под темы из банка
ошибок, без ошибок — по кругу.

## 108. Тексты «Рейтинга» — по коду

Шаг 1 утверждал «средний балл за последние недели». В коде срока давности
нет: средний процент по лучшей попытке каждого теста (`dedupeBestScores`).
Шаг 2 обещал, что прошлые недели «видно в истории». Истории нет: сотрудник
недели — сумма очков с понедельника, в понедельник всё с нуля.

Пузырь стоял над шапкой экрана и стрелкой указывал на верхнюю строку «SA».
Этого вида никто не видел — экран падал раньше. Перенесён под шапку, как
в остальных разделах.

## 109. Подсветка ждёт свою цель

Шаг «Карточка сверху» на главном вставал в поток **под «Книгой отзывов»**,
и стрелка указывала на неё. Причина: карточка трека ждёт уроки роли, а они
грузятся отдельным файлом. Подсказка появлялась раньше карточки, замер был
один, цели нет — пузырь навсегда становился страничным. Сильнее всего это
у нового сотрудника, у которого файл уроков ещё не в кэше, — ровно тогда,
когда подсказка и показывается.

Теперь `HintBubble` ищет цель каждый кадр до полутора секунд и пока не рисует
ничего. Не дождался — обычный страничный пузырь, как раньше.

## 110. Подсветка ждёт, пока экран доедет

Каждый экран въезжает с анимацией: `.sa-pagein`, сдвиг на 10 px, 0,28 с.
Карточка вопроса в «Ошибках» ещё и выезжает сбоку (`.sa-cardpage-r`, 26 px).
Замер в первые кадры ставил рамку мимо цели, и она так и оставалась.
Правка 100 убрала рывок между двумя замерами, но не сам сдвиг.

Перед замером `HintBubble` теперь ждёт конца конечных анимаций у цели и её
родителей (бесконечные вроде пульса не в счёт), не дольше 0,7 с. Замер в
браузере: рамка стоит на цели с точностью до пикселя.

## 111. Подсветка в «Работе над ошибками»

Шаг 1 — текст вопроса, шаг 2 — строка «этап закрепления N из 5».

Первая версия подсвечивала всю карточку вопроса. Она тянется до низа экрана:
рамка ничего не выделяла, а пузырь над ней вылезал за верхний край. Правило
на будущее: **цель подсветки не должна быть выше трети экрана**, иначе
пузырю негде встать.

Ссылки объявлены вверху компонента, до раннего `return` (пустой банк
ошибок), — иначе нарушится порядок хуков.

## 112. Просмотр в браузере

`tools/claude/`: сборка приложения через esbuild и сценарии в Chromium,
как на телефоне 390×844, без сети — приложение само уходит в офлайн-режим с
кэшем профиля. Снимки в обеих темах, сбор ошибок страницы, проверка
«появилась ли подсказка» и замер рамки. Обход всех 17 подсказок в двух
темах — чисто.

Чего просмотр не заменяет: шрифт вместо Georgia похожий (Lora) — переносы
строк могут отличаться; React 19 вместо 18; график смен без данных сервера
не открывается, его подсказки в браузере не перепроверены.

### Замечено, не исправлено

- «Задания дня»: у главы справочника всегда подписано «Курс: Сервировка»,
  даже если глава из курса про кофе или вино.
- Точка на иконке «Меню» ставится и когда не закрыта «пятёрка дня», а
  подсказка меню объясняет её только новыми блюдами.

# Правки 113–117

## 113. Чек-листы: подсказка с подсветкой

Раньше подсказки не было вовсе, и ключа в `data/hints.js` тоже. Добавлены
`checklist_staff` и `checklist_admin`. Сотруднику подсвечиваются вкладки
списков и полоска «сделано N из M», руководителю — полоска и «✎ Править».

Серверной части чек-листов в проекте нет, поэтому в текстах только то, что
видно из кода приложения: отметки хранятся за день, последняя фиксирует время
и имя, «Править» меняет список только своего ресторана. Что отметки видит вся
смена, НЕ утверждается — проверить нечем. Показано владельцу макетом до
сборки, возражений не было.

## 114. Колода бармена: ключи по ролям и тексты по коду

Второй шаг старого текста рассказывал сотруднику про «Свои коктейли», а
редактор есть только у руководителя (`canEdit`). Ключ разведён:
`cocktails_staff` — один шаг про «Знаю?», `cocktails_admin` — ещё и «Свои ›».

Первый шаг раньше описывал то, что и так видно («состав, пропорции, посуда»).
Теперь — правило, которого не видно: «Знаю?» — интервалы 1 → 3 → 7 → 30 дней,
ошибка возвращает карточку сразу (`mark` в `ui/cocktails.jsx`).

Пузырь стоит после шапки, прямым ребёнком блочного корня: шапка собрана из
трёх флекс-рядов, внутри любого из них он бы сжался (этим «другая структура
корня» и мешала шаблонной вставке).

## 115. «Сборка руками»: подсказка по коду

Старый текст: «на время» — это только «Час пик»; «ошибка не штрафует» — в
«Часе пик» она добавляет 10 секунд. Новый — про то, чего экран не говорит:
печать не сгорает, ошибка обнуляет только серию к «мастеру» (`recordRun`
никогда не понижает уровень); коктейль дня один на всю команду.

В файле несколько компонентов (`Play`, `GlassView`…). Состояние подсказки и
пузырь — в `BarLabScreen`, пузырь только на главном виде. Ссылки переданы
через помощника плиток свойством `ref` — `refcheck` научен это видеть.

## 116. «Час пик» не брал заказы из своей карты бара

На главном виде «Сборки руками» помощник плиток назывался `card` — так же, как
карта бара выше в компоненте. Внутри вида он её перекрывал:

- подпись «· СВОЯ КАРТА» горела всегда — `card` там функция, она всегда
  истинна;
- «Час пик» с этого экрана получал вместо списка функцию: условие «карта
  длиннее трёх» для неё не срабатывало, и заказы шли из всех коктейлей, а не
  из своей карты (Доп. 214 отсюда не работал; из экрана результата работал).

Помощник переименован в `tile`. Проверено в браузере: без карты подписи нет;
с картой из трёх коктейлей подпись есть, «Час пик» выдал заказ из неё.

## 117. Замечено, не исправлено

`GlassView` в `ui/bar-lab.jsx`: `preserveAspectRatio="xMidYEnd meet"` —
такого значения нет (есть `xMidYMax`), браузер пишет ошибку в консоль и берёт
значение по умолчанию. В квадратных рамках разницы не видно. Исправление
сдвинет бокал к низу там, где рамка шире бокала, — это изменение вида,
поэтому без согласования не трогал.

# Правки 118–122 — последние четыре готовых текста

## 118. «Моя статистика» (ключ stats)

Старый текст обещал, что «видно, какие темы даются, а какие проседают». Такой
разбивки на экране нет: профиль, шесть цифр, прогресс по ролям, последние
тесты. Новый — два шага с подсветкой, про то, чего экран не объясняет:

- звание под именем — из Книги отзывов (`rankOf` в `data/reviews.js`: страницы
  плюс печати × 3, то есть печать весит как четыре страницы);
- «Ролей завершено N/4» считает только четыре ступени; Хостес и Бар — отдельные
  треки, они в списке ниже, но в этот счёт не входят.

`a11y` в экране нет — пузырь берёт `T.a11y` (урок правки 106).

## 119. «Аккаунт» (ключ profile)

Оба старых шага были не про этот экран. Имя здесь не меняется — сам экран
говорит «обратись к администратору». Телефона на нём нет вовсе, так что шаг
о том, кто видит телефон, убран.

Новый шаг с подсветкой карточки: имя — из кода доступа, и по нему график
находит «мои» смены (`meToks` в `ui/schedule.jsx`); ошибка в имени — смены могут
не найтись. Это и есть причина, по которой стоит просить администратора.

## 120. Сертификаты

Из старого текста верно одно: сертификат не за отдельные уроки. «Сохранить
картинкой» не проверял — в новый текст не вошло. Новый — страничный: экзамен
открывается после всей роли; сертификат действует год, за месяц до срока здесь
появляется напоминание о переаттестации (`VALID_MONTHS`, 30 дней).

## 121. «Гость недели»

Идёт через `LiveDialogue` — общий экран всех диалогов. Подсказка включается
только по ключу, который передаёт `App.jsx` (`hintKey="weeklyGuest"`); без ключа
её нет — хук вызван всегда (правило хуков), но не включается, а список шагов пуст.

Старый текст: «настоящий гость» — гость сценарный; «правильных ответов
несколько» — в каждом шаге верный один. Новый — по коду: страница в Книге за
доведённый до конца разговор, а не за счёт; после каждого ответа разбор;
настроение гостя — полоска в шапке. Уйти гость может только в режиме pro,
«Гость недели» идёт без него — поэтому про уход ни слова.

Страничная, не с подсветкой: панель диалога въезжает снизу 1,1 с — дольше,
чем подсветка ждёт окончания движения (0,7 с).

## 122. Итог пункта 2 и обход

Все шесть «готовых» текстов подключены — и все шесть пришлось переписать по
коду. Обход в браузере теперь 26 подсказок; в двух темах чисто, 10 из них с
подсветкой. У просмотра появился `PORT` — темы можно гнать параллельно.

# Правки 123–126 — аудит подсказок

## 123. Аудит подсказок

28 ключей; 26 экранов в браузере — обе темы, два размера телефона (390×844 и
360×640), каждый шаг. Работают все: появляются, листаются, закрываются, после
перезапуска не возвращаются, пузырь нигде не выходит за экран, рамка на цели.
Отчёт — `docs/HINTS-AUDIT-2026-09-21.md`, инструмент — `tools/claude/hint-audit.mjs`.

Главное — тексты: 9 ключей из 28 утверждали то, чего приложение не делает.
Каждое утверждение сверено с кодом, неверные заменены (в `data/hints.js` рядом —
комментарий, где это видно в коде):

- книга отзывов — «пишут настоящие гости», «печати — сколько отзывов собрала
  команда»: на деле отзывы заготовлены и открываются за модули, печать — за экзамен;
- смена (обе роли) — «кто с тобой в зале, тап по лицу — номер», «кого не хватает»:
  карточка одна на обе роли и показывает только свою смену из графика, открытого
  последним (`sa_today_shift`);
- главный экран сотрудника — «справочник, меню, глоссарий»: новичок, который и видит
  подсказку, получает в ленте только справочник, «Первую неделю» и SOS;
- главный экран руководителя — три предложения вместо двух; «аналитика по смене»;
- команда — рекорды не здесь, а в «Рейтинге»; «кто давно не заходил» — в «Сотрудниках»;
- меню (обе роли) — метка на иконке это не только новые блюда, но и незакрытые
  «ежедневные пять».

Длина: на 360 px кнопка справа занимает треть пузыря, и шаги доходили до 7 строк.
Девять шагов сокращены — теперь 4–6 строк.

## 124. Контраст мелочей пузыря

Крестик: `#8F7B57` → `#A8966F` в тёмной (было 3,7:1), `#6E5C3C` → `#5E4E30` в
светлой (было 4,4:1). Подпись «ПОДСКАЗКА» в светлой — `#6B4E14` вместо золота
(было 3,4:1 у страничного пузыря и 4,25:1 у пузыря с подсветкой). Тот же цвет
крестика в `EmptyState` не тронут — там он для текста. После правки в пузырях
слабого контраста нет ни в одной теме.

## 125. Ошибка в собственном отчёте

В отчёте для руководителя я предложил «справочник, меню, глоссарий и инструменты
руководителя». Перед применением перепроверил: на главной вкладке ленту фильтрует
`learnOnly` — там справочник, меню, SOS и глоссарий, а инструменты руководителя
живут во вкладке «Команда». В применённом тексте их нет, отчёт исправлен.

## 126. Общий аудит экранов

Сделан по недоразумению (просили аудит подсказок), но пригодится:
`docs/AUDIT-2026-09-21.md` — 44 экрана-вида × 2 темы, контраст всех надписей и стекло
карточек. Функционально чисто; главное — контраст сходится к нескольким токенам
(`MUTED_3` в тёмной, золото текстом в светлой и др.).

## 127. Версия подсказок: 4 → 5

Решение владельца. Девять текстов были неверны (правка 123), и без новой версии
исправления увидели бы только новые сотрудники: у остальных отметка «видел» уже
стоит. Теперь все подсказки один раз покажутся всем заново.

Заодно исправлен комментарий у `HINTS_VERSION`: он утверждал, что вместе с
подсказками заново покажутся и карточки первого входа. Это не так — у них свой
ключ `sa_welcome_seen_v11` в `App.jsx`, и от версии подсказок они не зависят.
Проверено в браузере: под версией 5 подсказки показываются, отметка
`sa_hint_v5_…` ставится, после перезапуска не возвращаются.

## 128. Главная подсказка вставала поверх карточек первого входа

Запись владельца с другого устройства: при первом входе подсказка «Карточка
сверху…» появлялась одновременно с карточками «Четыре вкладки внизу / Твой трек…».
Затемнение подсказки перехватывало касания — карточки не листались, пока не
пролистаешь три шага подсказки, — а золотая рамка обводила размытый фон под
карточкой. Так было бы у каждого нового сотрудника.

Причина: значок наставника и тост про ошибки в `App.jsx` ждали `!welcome`, а
главная подсказка в `RoleSelect` о карточках не знала — её готовность была только
«роль выбрана». Раньше это маскировала гонка из правки 109: первый шаг вставал в
поток страницы и прятался под карточками. После правки 109 он честно встал с
подсветкой — поверх всего.

Теперь `App` передаёт `hintsReady={!welcome}`, и подсказка ждёт закрытия карточек —
и в готовности `useHintOnce`, и при отрисовке: `welcome` включается эффектом уже
после загрузки профиля, одного порога в готовности мало. Проверено в браузере на
обеих ролях и темах: пока карточки открыты — подсказки нет, «Пропустить» нажимается;
после карточек подсказка встаёт на карточку трека, рамка точно на цели. Обычный
вход (карточки уже видены) — без изменений.

## 129. Лента «под рукой»: ровно четыре — Справочник, Меню, Глоссарий, Чек-листы

Владелец заметил, что в ленте стало меньше разделов. Ничего не терялось: в
Доп. 133 (четыре вкладки) и 142 рабочие инструменты переехали во вкладки —
Чек-листы в «Смену», Наставничество, Новички, Аналитика, Собеседование и
Редактор — в «Команду», Сертификаты — в «Я», Колода — в Справочник.

Решение владельца: в ленте ровно четыре — Справочник, Меню, Глоссарий,
Чек-листы, в этом порядке, и подпись по очереди раскрывается, как раньше.
«Первая неделя» ушла во вкладку «Смена» — кроме сезонника-новичка до первого
урока: пока это его главный план, он остаётся под рукой.

Раскладка. Макет с чек-листами показал, что пятый инструмент уезжает под SOS,
а четвёртый с раскрытой подписью упирается в разделитель. Замер в браузере:
при кружках 40 px «Справочник» вылезал на 9 px (390 px) и на 7 px (360 px), а в
момент смены подписи — когда одна ещё закрывается, а другая уже раскрывается —
до 41 px. Исправлено:

- кружки 38 px, на узком телефоне (меньше 380 px) — 34 px; отступы ужаты;
- смена подписи по очереди: старая закрывается, потом раскрывается новая;
- ряд обрезается по краю, если шрифт на телефоне крупнее, — а не заезжает под SOS.

Замер каждые 150 мс в течение 17 с, 390 и 360 px, сотрудник и руководитель:
ряд не вылезает ни разу — ни в покое, ни при смене подписи. Подсказка главного
экрана (шаг 2) обводит новую ленту точно; её тексты обновлены под новый состав.

# Правки 130–136 — лазейка в тестах, «Находчивая жопка», аналитика по людям

## 130. Лазейка в тестах

Запись владельца: в тесте ответил неверно, нажал «‹» — тест снова свободен, можно
пройти заново, уже зная ответ, и так до 100%. Тест запирается, только когда пройден
на 70%+ и нажато «Продолжить ✓»; выход до этого ничего не сохранял.

Решение владельца — не наказывать, а поймать и мягко разыграть: секретная ачивка
тому, кто нашёл, и разбор его ошибок; руководителям — кто, где и как.

Сначала я пообещал отчёт «задним числом» по серверным записям ответов (этап 7). Это
было ошибкой: в тестах есть и ОФИЦИАЛЬНАЯ пересдача — меньше 70% или три ошибки,
кнопка «Вернуться к урокам». По записям на сервере честная пересдача выглядит так же,
как лазейка. Поэтому лазейку определяет приложение в момент выхода (`ui/loophole.jsx`):

- лазейка — «‹» посреди теста после ошибки, закрытое посреди теста приложение
  (ловится при следующем открытии), уход с результата 70%+ без «Продолжить»;
- не лазейка — официальный провал и выход без единой ошибки (отвлёкся на звонок).

## 131. Подсвеченный ответ терялся при выходе

Выбранный ответ сначала подсвечивается, а засчитывается через 0,6 с (верный) или
1,4 с (неверный — чтобы увидеть правильный). Уход с экрана таймер просто сбрасывал.
На записи владельца «‹» нажато через ~0,7 с после красной подсветки: верный вариант
уже показан, а ошибка не попала ни в тест, ни даже в серверную запись ответов этапа 7.
Нашла это сквозная проверка в браузере — до сборки.

Теперь при выходе подсвеченный ответ засчитывается сразу, до `onBack` (кнопка «‹»),
и при любом другом уходе с экрана. Заодно «Аналитика» больше не теряет брошенные ошибки.

## 132. Секретная ачивка «Находчивая жопка»

Название и первая фраза — владельца. Сдал тест после выхода — карточка:
«Ах ты, хитрая жопка ☺️ В тесте «…» ты вышел на N-м вопросе и прошёл его заново.
Находчивость засчитана — а ошибки первой попытки мы сохранили:» — и каждая ошибка:
что ответил, что верно, объяснение из урока. «Разобрать ошибки» ведёт в «Работу над
ошибками» (эти вопросы уже там — неверный ответ кладётся туда сразу). Карточка не
закрывается сама, в отличие от обычной ачивки: разбор надо успеть прочитать; если
её пропустили — покажется при следующем входе. Видит только сам сотрудник (у себя на
телефоне) и руководители; коллеги — нет.

## 133. «Аналитика»: вкладка «Люди» и лестница видимости

Кто, где и как ошибается за 90 дней: карточка на человека, по нажатию — вопросы, что
выбирал, что верно, сколько раз. Ключик у имени — нашёл лазейку. Кто кого видит —
решение владельца, отбирает сервер (лишнего телефон не получает):

- менеджер — официантов, хостес и барменов своего ресторана;
- руководящий состав — линейный персонал и менеджеров всех ресторанов;
- админ — всех.

«Лазейки» — по той же лестнице. Вкладок пять не помещались («Сводка» обрезалась на
390 px), поэтому «Темы» переехали внутрь «Сводки», под «Слабое место»; вкладки теперь
Люди · Вопросы · Лазейки · Сводка — помещаются и на 360 px. «Люди» открываются первыми.

## 134. Этап 15 — `supabase/supabase-stage15-quiz-retries.sql`

Запустить один раз в SQL Editor после заливки. Внутри: лестница `sa_rank`, запись
выбранного и верного ответа (`log_quiz_pick` — отдельным вызовом, чтобы старая запись
ответов работала и без этапа 15), таблица выходов `quiz_skips` с записью и отчётом
(`log_quiz_skip`, `quiz_skips_list`), отчёт «Люди» (`quiz_people`). Без этапа 15
приложение работает как раньше: новые вызовы тихо отклоняются, вкладки подсказывают,
какой файл применить. Что человек выбирал — видно с момента запуска: старые ошибки
только с вопросом.

## 135. Ошибка, которую поймала проверка областей видимости

`loopUk` стоял в зависимостях `handleQuiz`, а объявлен был ниже. Зависимости
вычисляются при отрисовке — приложение упало бы на первом экране. Объявление
перенесено выше, сразу за профилем.

## 136. Проверено

Логика лазейки — 10 сценариев. Сквозная проверка в браузере на настоящем тесте
(«Философия сервиса») — 18 утверждений: выход через 0,6 с после неверного ответа
записан и отправлен, после сдачи — карточка с ошибкой и верным ответом, тест
отмечен сданным, «Понятно» закрывает, после перезапуска не возвращается; честный
провал — не лазейка, карточки нет. Регрессия подсказок — 26 без ошибок. Скрипт
проверки — `tools/claude/e2e-loophole.mjs`.

# Правки 137–139

## 137. «Люди» и «Лазейки» писали «не включена», хотя этап 15 был запущен

Проверка после запуска показала 5 функций, 1 таблицу и 2 колонки — функции в базе
были. Но сервер API (PostgREST) на этом проекте не всегда сам замечает новые
функции — это уже случалось на этапе 9, там даже записана лечащая строка. Приложение
получало «такой функции нет» (PGRST202), а экран любую ошибку показывал как «не
включена». Лечится одной строкой в SQL Editor: `notify pgrst, 'reload schema';`.

- строка добавлена в конец этапа 15;
- «Аналитика» теперь различает: PGRST202 — «сервер ещё не видит новые функции» и
  эта строка; другая ошибка — её текст и код («пришли разработчику»); и только если
  ответа нет вовсе — «примени этап 15».

## 138. Карточка «Находчивая жопка» — вариант «Раскусили»

Владелец попросил сделать ачивку красивее. Настроение — «тебя раскусили», а не «ты
победил»: без конфетти и кубков. Показаны три варианта макетом; выбран «Раскусили»,
штамп — «ПОПАЛСЯ» (слово владельца):

- золотой медальон с ключом, по нему пробегает отблеск;
- через полсекунды наискосок падает красный штамп «ПОПАЛСЯ», карточка вздрагивает;
- название — золотым переливом, в одну строку (на узких экранах шрифт меньше);
- «Ах ты, хитрая жопка ☺️» — отдельной строкой курсивом;
- каждая ошибка — своя стеклянная плашка с метками «ТЫ ОТВЕТИЛ» и «ВЕРНО»,
  объяснение — цитатой; по кнопке «Разобрать ошибки» пробегает блик.

При «уменьшении движения» в настройках телефона штамп просто стоит на месте (иначе,
влетая из прозрачности, он не появился бы вовсе), а отблески скрыты — застывшие, они
выглядели бы светлыми пятнами. Проверено в обеих темах, на 360 px и без анимации;
сквозная проверка лазейки — 18 из 18.

## 139. Инструменты

`shoot.mjs`: шаг `waitFor` (ждать элемент) и сценарий с `reducedMotion` — режим без
анимации. `e2e-loophole.mjs` проверяет новые метки и штамп.

# Правки 140–143 — тайный зачёт ключей и баннер-загадка

## 140. Тайный зачёт ключей (этап 16)

Идея владельца: ачивку можно получить не один раз — пусть будет негласное
соревнование, кто соберёт больше ключей.

- Ключ — тест, пройденный через лазейку и потом сданный. Один тест — один ключ
  навсегда: сброс и повторный трюк нового ключа не дадут (уникальность на сервере).
- Зачёт — внутри своего заведения (решение владельца).
- Сотрудник видит только числа: свои ключи, место, сколько не хватает до первого.
  Имён нет — ачивка остаётся секретной. Имена — у руководителей в «Лазейках».
- Фразы владельца: «Ты хитрый, но до первого места — ещё N ключей»; первое место
  делят — «Ещё ключ — и оторвёшься»; первый — «Ты впереди всех хитрецов. Пока
  впереди 😉» (без запятой: «Пока, впереди» читалось бы как прощание).
- В карточке — плашка «Тайный зачёт ключей»: золотые ключи — свои, пунктирные —
  сколько не хватает до первого места.

Ключ уходит на сервер сразу после сдачи (`log_quiz_key` сразу возвращает место);
нет связи или этапа 16 — ключ ждёт в очереди, карточка покажется без плашки. Ачивки,
полученные до этапа 16, в зачёт не попадают (ключей на сервере нет — плашка скрыта).

## 141. Ачивка выезжает баннером-загадкой

Решение владельца: не выпрыгивать карточкой, а плавно выезжать баннером; тап —
раскрывается то, что уже сделано. Сверху выезжает стеклянная плашка: «Секретная
ачивка · Тапни, чтобы посмотреть · Кажется, ты кое-что нашёл…». Ключик в медальоне
поворачивается, как в замке, вокруг медальона дышит свечение, по плашке пробегает
блик. Тап — плашка уходит, снизу поднимается карточка, падает штамп «ПОПАЛСЯ».
Не тапнули за 12 секунд — плашка уезжает сама; ачивка не просмотрена и вернётся при
следующем входе. При «уменьшении движения» ключ не вертится, блики скрыты.

## 142. Лидеры по заведениям у руководителя

В «Лазейках» у каждого — число ключей, сверху — самые «ключастые». Метка «Лидер
тайного зачёта» — своя в каждом заведении (у админа с названием заведения).

## 143. Проверено

Сквозная проверка — 26 утверждений в трёх прогонах: лазейка (баннер до карточки,
ключ отправлен, тап раскрывает карточку с зачётом, после «Понятно» и перезапуска не
возвращается), честный провал (нет ни баннера, ни ключа), баннер не тапнули (уехал
через 12 с, вернулся при следующем входе). Анимация записана видео из браузера.
У просмотра появилась запись видео (`video: true` в сценарии).

# Правки 144–145

## 144. Кнопки карточки уезжали за край

Вопрос владельца: а если ошибок больше одной? Разбор показывает каждую ошибку —
на практике их не больше двух: третья по правилам теста сама его прерывает, это уже
честный провал. Но проверка показала, что с двумя ошибками и плашкой зачёта карточка
выше экрана (766 px на 390, 590 px на 360) и прокручивается внутри — кнопки
«Разобрать ошибки» и «Понятно» уезжали вниз, их не было видно.

Кнопки теперь прилипают к низу карточки, разбор прокручивается под ними и мягко
затухает — затухание само подсказывает, что ниже есть ещё. Проверено замером: кнопки
видны на 390 и 360 px, в обеих темах.

## 145. Несколько выходов из одного теста — один разбор

Раньше карточка показывала ошибки только первого выхода. Ошибка второго выхода —
такой же пробел, поэтому ошибки всех выходов собираются в один разбор, без повторов.
Текст: «ты выходил 2 раза — впервые на 4-м вопросе — и прошёл его заново.
Находчивость засчитана — а ошибки, которые ты стёр, мы сохранили». То же у
руководителя в «Лазейках»: «Ошибки, стёртые выходами» — всех выходов, по времени.

Сценарии логики — 11 (добавлены два: несколько выходов; разные ошибки в разных
выходах). Сквозная проверка — 26 из 26.

## 146. Повторная ачивка узнаётся

Вопрос владельца: а если это не первый раз — баннер тот же? Теперь повторы разные:

| Раз | Баннер (строка-загадка) | Штамп | Первая строка карточки |
|---|---|---|---|
| 1-й | «Кажется, ты кое-что нашёл…» | ПОПАЛСЯ | «Ах ты, хитрая жопка ☺️» |
| 2-й | «Снова ты? Ещё один ключ в копилке…» | СНОВА · ПОПАЛСЯ | «Опять ты, хитрая жопка ☺️» |
| 3-й | «Третий ключ. Это уже привычка?» | 3-Й РАЗ · ПОПАЛСЯ | «И снова здравствуй, хитрая жопка ☺️» |
| 4-й+ | «Ключ №N. Копилка тяжелеет…» | N-Й РАЗ · ПОПАЛСЯ | то же |

Со второго раза на медальоне — золотой кружок с номером ключа (и в баннере, и в
карточке). Тексты — в одном месте, `loopWords(n)` в `ui/loophole.jsx`. Номер — `n` у
записи ачивки (какая по счёту у этого сотрудника). Двухстрочный штамп выше обычного —
шапка карточки для него опускается на 12 px, иначе карточка, прокручиваясь внутри,
срезала бы верхнюю строку (было на первом снимке, поправлено до сборки).

Логика — 13 сценариев (`tools/claude/loop-logic-test.mjs`), сквозная — 26 из 26.

# Правка 147 — «Аналитика» в языке «морозного льда»

Просьба владельца: сделать раздел руководства визуально приятным (скрин: стена мелкого
текста). Меняется только отрисовка — данные, запросы и лестница видимости прежние.

- `ui/analytics-kit.jsx` — «кирпичики» в языке ачивки: аватар с инициалами в золотом
  кольце (цвет кольца — как у человека дела), кольцо-индикатор, полоса по тяжести
  (золото → янтарь → красный), дорожка «где вышел из теста», стеклянная плашка ошибки
  с метками «ВЫБРАЛ / ВЕРНО», подпись раздела с золотыми линиями, иконки линией,
  график-искра. Кольца и полосы при открытии заполняются; при «уменьшении движения»
  стоят сразу. У каждого цвета — вариант для светлой темы.
- Верх экрана — «Динамика» (выбор владельца из трёх вариантов макетом: пульс, динамика
  и рестораны, план брифинга): кольцо недели, изменение к прошлой неделе в пунктах
  («▲ +9», не «9%» — было 68%, стало 77%), «без изменений», если ноль; «на этой неделе
  тестов ещё нет» — если так; график среднего результата за 6 недель.
- «Рестораны · средний результат» — полосы по заведениям, лидер со звёздочкой; только
  тем, кто видит больше одного ресторана (админ, руководящий состав).
- «Люди»: аватар, метки должности и ресторана, число ошибок крупно, полоса «где
  сосредоточены ошибки»; в раскрытии — плашки «ВЫБРАЛ / ВЕРНО».
- «Лазейки»: карточка ачивки, «Тайный зачёт ключей» по заведениям (места, ключи-
  иконки), у тестов — «сдал · N%» / «пока не сдал» и дорожка с красным делением там,
  где вышел.
- «Вопросы»: золотые номера у первых трёх, крупный процент, цветная полоса, метки.
- «Сводка»: «Слабое место» с кольцом, «Уснули» — аватарами, темы — цветными полосами.

Проверено: все вкладки в обеих темах без ошибок, узкий экран 360 px, вид админа и
менеджера (у менеджера нет «Ресторанов»).

# Правка 148 — «function whoami(text) does not exist» в этапах 15–16

Скрин владельца: «Аналитика → Люди» — «Сервер ответил ошибкой: function whoami(text)
does not exist (42883)». Точный текст виден благодаря правке 137: раньше экран написал
бы просто «не включена».

Причина — моя: на этой базе `whoami` принимает токен не текстом, а внутри SQL тип сам не
подгоняется. Ровно это уже случалось (Доп. 155) и чинилось этапом 11c: переходник
`whoami_txt(text)` и перевод на него всех функций. Этапы 15 и 16 написаны позже и звали
`whoami` напрямую — хотя в шапке этапа 15 я сам сослался на 11c, но не прочитал его.

- во всех функциях этапов 15 и 16 — `whoami_txt(p_token)` (4 + 2 вызова);
- в начале обоих файлов — подстраховка: нет переходника — создаётся по фактической
  подписи `whoami`, как в 11c; есть — не трогается;
- `supabase/README.md` — правило для новых этапов: только `whoami_txt` и `notify` в конце.

Пока ошибка была, серверные вызовы этапа 15–16 (выбранный ответ, выходы-лазейки, ключи)
отклонялись и не записались; записи ответов этапа 7 шли как обычно — «Люди» покажут
ошибки за этот период, но без выбранного варианта.

# Правка 149 — баннер «морозный лёд», ниже; карточку не закрыть случайным тапом

Запись владельца и просьба: баннер — в стиле «морозный лёд», почти прозрачным, и чуть
ниже; и баг: «тапнул на баннер — следующая страница просто схлопнулась».

- Баннер — почти прозрачное стекло: под ним размыто видна страница (blur 18px), изморозь
  мелкими искрами, блик по верхней кромке, светлый контур; у текста — тень для
  читаемости на любом фоне. Проверено над логотипом (главный) и над картинкой модуля,
  в обеих темах.
- Опущен под верхнюю строку приложения («SA · Для чтения») — раньше закрывал её.
- Баг. Сам тап в запись не попал; у меня путь «сдал → баннер над модулем → тап →
  карточка → Разобрать ошибки / Понятно» проходит, покадровое видео — без схлопывания
  (экран модуля не прыгает: до тапа баннер просто закрывал верхнюю строку). Вероятная
  причина на настоящем телефоне: после тапа баннер 0,2 с гаснет, карточка ещё не встала —
  рука тапает второй раз, и тап попадает в затемнение вокруг карточки, которое её
  закрывало (ачивка при этом считалась просмотренной). Теперь первые 0,8 с тап мимо
  карточки ничего не делает; кнопки работают сразу. Проверено: тап мимо через 0,45 с —
  карточка на месте, через 1,2 с — закрывается.

Сквозная проверка дополнена путём «как у владельца» (баннер над модулем, тап, «Разобрать
ошибки», экран держится) — 33 утверждения.

# Правка 150 — карточка ачивки тоже «морозный лёд»

Просьба владельца: развёрнутую страницу — так же, как баннер.

- Карточка — полупрозрачное стекло: сквозь неё размыто видна страница (blur 22px),
  блики по углам, светлый контур, блик по верхней кромке; у текста — тень для
  читаемости. Изморозь на большой тёмной карточке тише, чем на баннере: при полной
  силе она рябила при чтении.
- Фон вокруг затемнён мягче и чуть размыт — карточка «висит» над экраном.
- Полоса кнопок — матовая: то, что прокручивается под ней, размывается, и размытие
  плавно нарастает сверху вниз (маска), без жёсткой кромки.
- Чернила штампа чуть ярче: на стекле мелкое «СНОВА» давало 4,3–4,5 при норме 4,5.

Контраст — замер каждой надписи против реального фона под стеклом
(`tools/claude/card-contrast.mjs`), обе темы, 390 и 360 px: в норме всё, кроме строк,
которые в этот момент ушли под матовую полосу кнопок, — они и должны гаснуть, пока их
не прокрутили. Сквозная проверка — 33 из 33.
