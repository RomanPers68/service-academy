# Что изменено в этой версии (аудит от 17.08.2026)

Все правки точечные, логика приложения не тронута.

## 1. data/glossary.js
Удалён дубль термина «Стрейнер» (был в двух категориях).
Оставлена версия в «Вино и бар» — рядом с шейкером и барной ложкой.
Чипы терминов в «Сборке» продолжают находить статью.

## 2. App.jsx — гонка загрузки прогресса
Эффект загрузки `progress` из Supabase фильтрует строки по известным
урокам (allValidIds), а СПГ-модули догружаются лениво. При быстром
ответе сервера прогресс роли «Хостес» отфильтровывался как неизвестные
id и перетирал localStorage. Теперь фильтрация ждёт loadSpgModules().

## 3. ui/build.jsx — перезапуск анимаций в «Сборке»
Визуальные носители (Vessel, Station, IcePath и т.д.) объявлены внутри
BuildRunner и рендерились как <Carrier /> — новый тип компонента на
каждый рендер, React пересоздавал их DOM целиком: анимация заливки,
льда и пузырьков перезапускалась даже при открытии чипа глоссария.
Теперь носители вызываются функциями ({Carrier()}), DOM сохраняется,
«fresh»-анимации играют только при реальном появлении нового слоя.
Плюс убран задублированный ключ color в StepList.

## Рекомендации без правок (на твоё решение)
- css.js: добавить `.sa-aiinput-light::placeholder` (стиль есть только
  для тёмной темы; в светлой плейсхолдер — браузерный серый).
- /api/assistant: нет rate-limit и проверки сессии — эндпоинт открыт,
  любой может расходовать токены. Стоит проверять sa_session_token.
- У 25 из 68 шагов «Сборки» термин без статьи в глоссарии (чип плоский):
  деэскалация, инвентаризация, тайминг, гигиена, допродажа и др.

# Дополнение (реализация рекомендаций)

## 4. data/glossary.js — термины для чипов «Сборки»
Добавлено 15 терминов: Деэскалация, Эскалация, Допродажа, Рекомендация,
Безопасность гостя, Стандарт, Тайминг, Гигиена, Инвентаризация,
Учёт по уровню, Приоритет выдачи, Баланс вкуса, Словарь вкуса,
Подача вина, Стекло. Плоских чипов стало 2 вместо 25 («выбор» и
«забота» оставлены подписями сознательно — это не термины).
Проверено скриптом: ни один новый термин не перехватил чужой чип.

## 5. api/_auth.js (новый) + api/menu-import.js + api/assistant.js
Оба Vercel-эндпоинта теперь требуют sa_session_token (проверка через
тот же RPC whoami, что в приложении) и имеют rate-limit в памяти
инстанса (menu-import: 8/час, assistant: 30/час). Все проверки идут
ДО обращения к Claude API — чужой запрос не тратит токены.
Клиенты обновлены: ui/menu-trainer.jsx и lib/assistant.js передают токен.
ВАЖНО: после деплоя проверь импорт PDF под своим аккаунтом — если
у тебя в Supabase RPC whoami переименован, поправь имя в api/_auth.js.
Живые AI-фичи (ai-chat, ai-hr в Supabase Edge Functions) не тронуты —
у них своя проверка токена и лимиты уже были.

## 6. ui/css.js
Явное правило .sa-aiinput-light::placeholder (близнец -dark).
Визуально ничего не меняет: каскад .sa-light уже красил плейсхолдеры —
исходная рекомендация была перестраховкой, правило добавлено для
симметрии и самодокументации.

# Дополнение 2 — улучшения «Сборки» и снятие BETA

## 7. Результат Сборки идёт в общий зачёт (главное)
ui/build.jsx: BuildRunner получил проп onResult(right, total) — вызывается
при выходе на итоговый экран каждого прогона (включая «Собрать заново»).
App.jsx: новый recordBuildResult — зеркало логики звёзд практики:
шкала едина (0 ошибок → 3⭐, одна → 2⭐, больше → 1⭐), сохраняется
только улучшение, тот же localStorage sa_practice_stars и тот же RPC
save_practice_stars. Звёзды Сборки автоматически попадают в лидерборд
(«Мастер практики») и статистику — отдельного хранилища нет.
На итоговом экране под печатью появился ряд ⭐⭐⭐ и подпись
«лучший результат идёт в общий зачёт».

## 8. Новые шаги про пропорции и баланс (data/builds.js)
- Негрони, шаг «Формула» (2-й): пропорция 30/30/30 по памяти — до
  вопроса об инструменте. Формулу знают наизусть, в час пик в карту
  не подглядывают.
- Дайкири, шаг «Проба» (6-й): проба из шейкера кислит — правка баланса
  маленькими шагами до подачи. Учит тому, что рецепт — отправная точка.
Оба шага без layer — визуал бокала не тронут (слои не переливаются).
Оба ведут чипом на новую статью «Баланс вкуса». Теперь 70 шагов в 13
сценариях, целостность проверена скриптом.

## 9. data/roles.js — снят флаг beta у роли «Бар»
Вернуть бейдж BETA: добавить `beta: true` обратно в строку роли bar.

## Идеи на следующий заход (не реализовано)
- Витрина печатей: страница в Книге отзывов с лучшими печатями по
  каждому сценарию Сборки (данные уже копятся в practice_stars).
- «Пересобрать» с фокусом: подсветить в StepList шаги, заваленные в
  прошлый раз (прыжок в середину невозможен — визуал кумулятивный).
- Сценарий «Эспрессо-мартини» или «Пина колада» для mod bar2 — пул
  реиграбельности вырастет без новых механик.

# Дополнение 3 — джус и полировка «Сборки»
ui/css.js + ui/build.jsx:
- jolt: носитель вздрагивает при ошибке (красная вспышка по контуру);
- nudge: лёгкий кивок предмета при верном ответе — глаз переводится с
  кнопки на бокал;
- heroServe + блик: финальный кадр «подаётся» — подъезд снизу и проход
  блика по собранному напитку (задействован готовый .sa-bld-shine);
- await: до первого решения пустой носитель тихо «дышит» — приглашение;
- анти-спойлер: подпись бокала («хайбол»/«купе») скрыта до первого
  ответа — она выдавала ответ на вопрос «в чём подаёшь?». Теперь до
  решения там «собери меня», после — имя бокала как подтверждение.
Все анимации уважают prefers-reduced-motion, обе темы, семь носителей.

# Дополнение 4 — контрольный проход по всему коду
Проверено и подтверждено целым: кросс-ссылки данных («Гость недели» →
диалоги, отзывы модулей → модули, скиллы → роли, меню → рестораны),
img-маркеры справочника → реестр иллюстраций (37/37), все обращения к
семи наборам иконок в 19 файлах, квизы справочника, 70 шагов Сборки.

Две находки исправлены:
- data/builds.js + ui/build.jsx: механика strain — с шага процеживания
  лёд исчезает из бокала. Дайкири больше не стоит на финале в купе с
  кубиками (шаг «Подача» учит: «лёд остаётся в шейкере», теперь визуал
  это показывает). Свойство переиспользуемо для будущих сценариев.
- ui/build.jsx: блик «подачи» на итоге играет только при чистом напитке
  (!spoiled) — праздничный глянец на вердикте «Пересобрать» звучал фальшиво.

# Дополнение 5 — дорожная карта, шаг 1 (по порядку)

## 10. scripts/check-content.mjs — секция 6
Весь ручной аудит теперь живёт в проекте: проверки «Сборки» (4 варианта,
один ok, fb/cost, слои ≤92%), buildId/dialogueId → существование,
next-переходы диалогов, пул «Гостя недели», дубли глоссария,
меню ↔ рестораны + файлы картинок, img-маркеры → реестр иллюстраций.
Запуск: npm run check:content. Прогнан — зелёный.

## 11. App.jsx — банк ошибок стал персональным
"sa_mistakes" → "sa_mistakes_Имя_Фамилия" (как sa_completed и др.).
Миграция: при отсутствии персонального ключа один раз читается старый
общий. Заодно сохранение переехало из трёх setState-апдейтеров в единый
persist-эффект (по образцу saved/examResults) — апдейтеры стали чистыми,
двойная запись в StrictMode ушла. На общем устройстве ошибки больше
не перетекают между сотрудниками.

## 12. Книга отзывов — «Витрина сборки» 🍸
Новая вкладка между ролями и «Гостем недели»: одна бумажная страница
со строками всех build-уроков роли «Бар» — мини-печать, вердикт
(безупречно / с замечанием / пересобрать / ещё не собрано) и звёзды.
Данные — из общего контура practice_stars (App передаёт проп),
отдельного хранилища нет. Обновляется само после каждого прогона.

## 13. docs/telegram-reminders.md
Готовая к деплою заготовка напоминаний: SQL для sa_set_tg (корень в
App.jsx уже вызывает его), Edge Function remind с ботом, RPC выборки
«активен, но не заходил 2–7 дней», расписание и кнопка в мини-апп.
Помечено как заготовка — прогнать на тестовом пользователе.

## Отложено осознанно (нужно твоё участие)
- Переход личности name|surname → profile.id: таблица scores на сервере
  ключуется name/surname — миграция затрагивает БД, вслепую не делаю.
- Разбиение screens.jsx (4561 строка): механически возможно через
  barrel-реэкспорт (App.jsx не изменится), но требует локального
  `npm run build` для проверки — сделаю следующим заходом, если
  подтвердишь, что сможешь собрать проект после.

# Дополнение 6 — разбиение screens.jsx (barrel-паттерн)

Монолит ui/screens.jsx (4561 строка) разнесён на 7 доменных файлов:
- screens-gamification.jsx — ачивки, рейтинг, задания, статистика, профиль
- screens-team.jsx        — «Команда», вход по коду, аккаунт
- screens-roleselect.jsx  — главный экран выбора роли и жетоны
- screens-admin.jsx       — чек-листы, онбординг, аналитика, редактор
- screens-learning.jsx    — дом роли, модуль, урок, глоссарий, ошибки
- screens-dialogue.jsx    — живой диалог
- screens-exam.jsx        — экзамен и сертификаты

ui/screens.jsx стал barrel-файлом (13 строк): публичный API один в один,
App.jsx НЕ менялся. Код перенесён БЕЗ изменений — доказано посимвольным
сравнением с эталоном (4274 содержательные строки идентичны; потеряны
только пустые строки-разделители на 5 стыках). Единственная правка:
_estMins/_fmtMins получили export (нужны roleselect из learning).
Межгрупповых циклов нет (3 однонаправленные связи). Финальный судья —
сборка Vercel: если что-то не сойдётся, деплой просто не пройдёт и в
проде останется прежняя версия.

# Дополнение 7 — вычитка всех текстов роли «Бар»
Прочитан весь корпус: 6 модулей (лекции, тесты), 13 сценариев Сборки,
банк собеседования бармена — ~170 КБ текста, вручную + автопаттерны
(30+ типовых ошибок: тся/ться, слитно/раздельно, барные термины и др.).
Найдено и исправлено:
- «в льду» → «во льду» — 5 мест (builds.js ×3, modules.js ×1,
  candidate-questions.js ×1); перед формой «льду» предлог всегда «во»
- builds.js, пиво: «Пиво выдаёт бар с головой — и твой выдал хороший»
  → «…и твоё выдало хороший бар» (рассогласование: подлежащее «пиво»)
Остальной текст чист: пунктуация, тире/дефисы, ё, терминология
(джиггер, стрейнер, гарниш, мизанпляс) — везде единообразны.

# Дополнение 8 — правка двух ремарок вычитки
- builds.js: «Негрони не прощает техники» → «не прощает ошибок в технике»
- modules.js: Пина колада убрана из спорных контекстов — в семействе
  сауэра заменена на Писко сауэр (бесспорная классика семьи), в списке
  шейка — на Эспрессо-мартини (узнаваемая шейк-классика). Осталась
  только неверным вариантом в вопросе про стир — там она на своём месте.

# Дополнение 9 — убран дубль «Бар» из блока «На подходе»
ui/screens-roleselect.jsx: плитка-анонс «Бар · НОВОЕ» времён запуска
трека удалена — бар давно живёт в основных ролях, и дубль на главной
только путал. Механизм «НОВОЕ» оставлен спящим (const active = false
с комментарием): когда следующий трек (Кухня) станет рабочим, одна
строка снова включит кликабельный анонс с бейджем.

# Дополнение 10 — рейтинг приведён к «Морозному следу»
ui/screens-gamification.jsx: фильтр должностей в «Рейтинге сотрудников»
переведён с ручных кнопок (цветная плашка на каждую должность: зелёная,
лососёвая, фиолетовая — наследие до морозного редизайна) на LiquidSegment
— то же жидкое стекло с золотым акцентом, что во вкладках Книги отзывов.
В светлой теме акцент — тёмное золото (дефолты компонента,
как в нижней навигации), иконки синхронизированы. Бонус: линза перетаскивается пальцем между должностями, как в
нижней навигации. Цвета должностей остались у иконок ролей в карточках
игроков — там они несут смысл, а не спорят с темой.

# Дополнение 11 — Справочник: курсы «Кофе» и «Бар и коктейли»
Заглушки «скоро» в хабе Справочника ожили — по архитектуре автора:
в уроках роли — ремесло смены, в справочнике — эрудиция для всей команды.

data/reference.js:
- «Кофе» (7 глав · со схемами): зерно и обжарка, эталон эспрессо и
  настройка помола, молоко и микропена (60–65 °C), классика и пропорции,
  альтернативное молоко и аллергены, чистота машины как ингредиент вкуса,
  истории кофейной классики (капуцины, американо и солдаты, раф и
  Рафаэль, флэт уайт, три смысла слова espresso) с фразами «Гостю: …».
- «Бар и коктейли» (6 глав · истории классики): канон IBA, истории
  аперитивов (Негрони, Апероль-шприц, Мартини), сауэров и тропиков
  (Дайкири/Хемингуэй, Маргарита, Пина колада, Май тай), вечерней
  крепости (Олд фэшн, Манхэттен, Эспрессо-мартини), безалкогольная
  классика, формула рассказа гостю за 15 секунд.

ui/reference-illustrations.jsx: три новые линейные иллюстрации в стиле
существующих бокалов — espresso_cup, milk_pitcher, coffee_ratio
(диаграмма эспрессо/капучино/латте), обе темы.

Подключение: ReferenceSection (карточки, роутинг), search (поиск по
главам), reference-daily («глава дня» теперь выпадает и из новых курсов
— резолвер курса нашёл их автоматически). check:content зелёный:
40 img-маркеров, 26 глав, id уникальны.

# Дополнение 12 — поиск в Справочнике + ассистент знает Справочник
1) ui/ReferenceSection.jsx: в хабе Справочника появилась строка поиска.
   Открывает готовый SearchScreen (ui/search.jsx — он был написан, но
   не подключён ни к одному экрану!) в режиме «только справочник»;
   результат ведёт сразу в главу (openById по всем четырём курсам).
2) lib/reference-context.js (новый) + ui/assistant.jsx: клиентский RAG.
   Перед отправкой вопроса ассистенту главы Справочника скорятся по
   словам запроса; топ-2 выдержки (абзац с совпадением + 📌-вывод,
   до ~1300 симв.) подмешиваются в копию последнего сообщения. Сервер
   (Edge Function ai-chat) не менялся — он видит просто подробный вопрос.
   История чата в UI остаётся чистой; на нерелевантные вопросы контекст
   не добавляется (порог скоринга + стоп-слова). Смоук-тест: капучино/
   латте, температура молока, Негрони — точные попадания.

# Дополнение 13 — «График смен»: фикс замка и состав смены
ui/schedule.jsx:
1) Фикс замка-ловушки: тап по клетке закреплял её даже пустой — случайный
   тап навсегда прятал клетку от генератора, а снять точечный замок было
   нельзя (только сброс всех закреплений месяца). Теперь доциклил до
   пустой — замок снимается, клетка возвращается генератору.
2) Вид сотрудника: тап по рабочему дню раскрывает «В смене с тобой» —
   состав дня по позициям (менеджер, бар, официанты...), у коллег в
   другой смене — её буква в скобках. Первый вопрос смены «кто сегодня
   со мной?» теперь решается без звонка менеджеру. Данные из уже
   загруженного plan, ни одного нового запроса.

# Дополнение 14 — «График смен»: сегодня, обмен, экспорт, заметки
ui/schedule.jsx, четыре функции:
1) Карточка «Сегодня» (менеджер): при открытом текущем месяце сверху —
   кто в смене сегодня по позициям с буквами смен, недоборы красным с «!»,
   старший дня. Ответ на главный вопрос менеджера без поиска колонки.
2) Обмен сменами (менеджер): кнопка «Обмен» → тап по двум клеткам —
   их содержимое меняется местами (обе закрепляются). Работает и между
   людьми, и между днями одного человека. Выбранная клетка светится.
3) «Сохранить смены картинкой» (сотрудник): персональный месяц в PNG —
   смены с временем, часы, итоги и заметки (✎). Через системное меню
   отправляется себе или в семейный чат.
4) Личные заметки к дням (сотрудник): тап по любому дню → поле заметки
   (чаевые, важные события, до 200 симв.). Маркер ✎ у дней с заметками.
   Хранятся в localStorage устройства — персональный блокнот, сервер
   о нём не знает; это честно написано под полем.
Бонус: раскрытие дня теперь работает и для выходных (ради заметок).

# Дополнение 15 — «График смен»: телефоны для связи в смене
ui/schedule.jsx: у сотрудника появилось поле «телефон» в настройках
заведения (менеджер заполняет, хранится в cfg.staff как и остальное).
Везде, где показаны люди смены, имя с заполненным телефоном становится
звонком по тапу (tel:-ссылка, номер чистится от скобок и дефисов):
- «старший: Имя» в списке смен сотрудника;
- список «В смене с тобой» в раскрытом дне;
- карточка «Сегодня» у менеджера.
Имя со звонком помечено пунктирным подчёркиванием. Сценарий: проспал,
заболел, нужна замена — связь в два тапа, без переписки. В подсказке
секции «Сотрудники» честно сказано, что телефон виден коллегам.

# Дополнение 16 — «График»: полировка настроек и лечение каретки
ui/schedule.jsx:
1) Каретка: График живёт в position:fixed-слое, а iOS при открытии
   клавиатуры прокручивает страницу ПОД ним — WebKit рисует каретку со
   смещением от поля. Лечение: при фокусе страница возвращается на
   место (window.scrollTo(0,0)) перед подводом поля к центру; на blur —
   страховочный сброс через onBlurCapture на обёртке.
2) Поля ввода: 13.5px → 16px (как у поиска и ассистента) — крупнее
   тап-цель, ноль сценариев зума-на-фокус; Enter закрывает клавиатуру
   (enterKeyHint="done" + blur), числовые поля чуть шире под новый кегль.
3) Свёрнутые секции показывают сводку настроек вместо описания:
   «с 12:00 до 26:00», «У Д В К · авто: 3», «12 / 13 / 14 чел.»,
   «пик: пт,сб · до 5 подряд · отдых 11 ч», «12 чел. · в отпуске: 2».
   Обзор всей конфигурации — без единого тапа.
4) Карточки сотрудников свёрнуты в строки «Имя · должность · норма ч»
   с метками (✆ телефон, отпуск, выходные) — вместо простыни из десяти
   полей на человека. Тап раскрывает редактор; новый сотрудник
   открывается сразу для заполнения.

# Дополнение 17 — контрольная полировка нововведений
Полный проход по всему добавленному за сессию. Проверено и цело:
импорт-резолв всего проекта, структура шести самых правленых файлов,
константы Витрины, защита dishesFor, stopPropagation заметок и звонков,
check:content. Три находки исправлены:
1) ui/schedule.jsx: CallName/telHref вынесены на уровень модуля —
   объявленные внутри экрана, они получали новую идентичность каждый
   рендер, и React пересоздавал DOM ссылок-звонков впустую (та же
   болезнь, что лечили у носителей Сборки).
2) ui/schedule.jsx: выбор клетки в режиме «Обмен» сбрасывается при
   загрузке месяца — клетка старого месяца не может обменяться с
   клеткой нового.
3) Поиск в Справочнике: теперь ищет и по меню ресторана (profile
   прокинут App → ReferenceSection → SearchScreen), а текст пустого
   состояния честно описывает область: «по главам справочника,
   глоссарию и меню ресторана» (новый проп scopeText).

# Дополнение 18 — генератор графика стал умнее (lib/schedule-gen.js)
Алгоритм вынесен из ui/schedule.jsx в чистый модуль и прокачан:
1) canPlace — единый валидатор всех жёстких правил (одна точка истины);
   отдых теперь проверяется с ОБОИМИ соседними днями, серия «подряд» —
   через день целиком, а не только назад.
2) Гибкие слоты — главный рычаг: потребность без разбивки закрывается
   любой авто-сменой (как и считает аудит). «Не раньше 14:00» больше не
   оставляет дыру в Дне — человек выходит в Вечер.
3) Мультистарт: 24 прогона с разными тай-брейками, побеждает лучший
   счёт (недоборы → недоработки → справедливость пиков).
4) Ремонт: оставшиеся дыры закрываются перестановками — ротация смен
   внутри дня и перенос смены в соседних ±3 днях с передачей коллеге.
5) Фикс: занятый на кейтеринге больше не «закрывает» потребность зала.
Проверено стендом в Node на 120+ прогонах (60 случайных заведений,
2 конфигурации вручную): нарушений жёстких правил — НОЛЬ во всех,
ручные закрепления целы. Итог против старого генератора на тех же
60 заведениях: недоборы 1449 → 741 (−49%), недоработка
13927 → 10754 ч (−23%). После «Заполнить черновик» экран говорит
итог: «все смены закрыты» или «не хватило людей на N смен».

# Дополнение 19 — полировка нового генератора
lib/schedule-gen.js, три правки по самопроверке:
1) Возвращена поддержка устаревшего «вечернего усиления» (cfg.evening):
   поля в настройках уже нет, но сохранённые конфиги старых заведений
   могли его нести — старый генератор его уважал, новый терял. Покрыто
   тестом: усиление «В по пятницам» соблюдается.
2) Стабильная сортировка слотов: рандом внутри компаратора заменён
   заранее назначенными ключами (нестабильный компаратор — антипаттерн).
3) При отсутствии авто-смен модуль возвращает plan:null — экран молча
   выходит, как раньше, вместо ложного «все смены закрыты».
Регрессия на тех же 60 заведениях: 741 недобор / 10754 ч — цифры
дополнения 18 подтверждены, нарушений жёстких правил по-прежнему ноль.

# Дополнение 20 — ядро обучения: тест получил обратную связь
ui/screens-learning.jsx:
1) Тест урока больше не «слепой»: тап по варианту подсвечивает выбранный
   и правильный ответы (стиль и цвета — ровно как в «Работе над
   ошибками», единый язык ответов в приложении), с хаптикой и
   автопереходом: верный — 0.6 с (держим темп), неверный — 1.4 с
   (глаз успевает увидеть правильный). Двойной тап заблокирован,
   таймер чистится при уходе с экрана. Экзамен сознательно оставлен
   слепым — экзамену положено.
2) Финал практики: многоточие в разборе ставится только когда текст
   действительно обрезан.

# Дополнение 21 — финальная полировка всего приложения
Полный контрольный проход (55 файлов): структура, импорт-резолв всего
проекта, CSS-классы, check:content — всё зелёное (lib/utils.js — давний
ложный флаг чекера на regex, файл идентичен эталону). Точечные правки:
- превью вопроса в редакторе контента и превью блюда в тренажёре меню
  получили loading="lazy" + decoding="async" (ниже сгиба);
- мимодзи — decoding="async" без lazy (они над сгибом, lazy дал бы
  мигание при листании карточек);
- логотипы намеренно без lazy (шапки экранов);
- ui/screens-roleselect.jsx: тернарник с одинаковыми ветками
  (a11y ? LOGO_SRC_DARK : LOGO_SRC_DARK) упрощён до LOGO_SRC_DARK
  с комментарием — поведение не изменилось ни на пиксель.

# Дополнение 22 — аудит орфографии новых курсов
Тексты «Кофе» и «Бар и коктейли» (16,6 тыс. знаков, 13 глав) прошли
двойную проверку: автопаттерны (25+ правил: термины, тире, дубли,
частые ошибки) — 0 находок; единообразие 17 имён напитков с уроками,
Сборкой и глоссарием — разнобоя нет; вычитка глазами — 4 правки:
«по-старинке» → «по старинке» (наречие без дефиса), «в впечатление» →
«во впечатление», «за соседним стулом» → «на соседнем стуле»,
«не сладкий» → «несладкий»; плюс «97 %» → «97%» под формат приложения.

# Дополнение 23 — HOTFIX: краш экрана «График» на проде
ui/schedule.jsx: блок личных заметок (Дополнение 14) был вставлен выше
объявления mkey, а массив зависимостей его эффекта вычисляется прямо
при рендере — TDZ-ошибка "Cannot access 'Y' before initialization"
(минификатор переименовал mkey в Y) роняла весь экран при открытии.
Блок перенесён ниже объявления mkey с предостерегающим комментарием.
Урок: статические проверки не ловят порядок инициализации — по всем
вставкам сессии прогнана TDZ-охота по deps-массивам, других нет.

# Дополнение 24 — HOTFIX: линза вкладок Рейтинга промахивалась
ui/screens-gamification.jsx: в equal-режиме LiquidSegment линза считает
позицию математикой равных долей, а подписи должностей разной ширины —
смещение копилось слева направо («Менеджеры» подсвечивались между
вкладками, «Руководство» обрезалось за краем). Переведено на режим
Книги отзывов (equal={false} + scroll): линза встаёт по реальным
координатам вкладок, дорожка прокручивается, все должности доступны.

# Дополнение 25 — HOTFIX: кнопка ассистента «Открыть Справочник» вела в Глоссарий
Модель на сервере ставила маркер [[go:glossary|Открыть Справочник]] —
её промпт писался до появления Справочника, ключа reference она не
знает (клиент при этом знал его всегда). Лечение с клиента, двумя
слоями, Supabase не трогали:
1) lib/reference-context.js: RAG-инструкция теперь прямо учит модель
   ставить [[go:reference|Открыть Справочник]] и предупреждает не
   путать с go:glossary.
2) ui/assistant.jsx: страховка парсера — если ключ glossary пришёл с
   подписью про Справочник, верим подписи (намерению модели), а не
   промахнувшемуся ключу, и ведём в reference.
App принимает reference в белом списке переходов — проверено.

# Дополнение 26 — ассистент знает меню ресторана
Вопрос «состав салата?» уводил в Справочник: RAG кормил модель только
главами, про блюда она была слепа — хотя меню живёт в приложении.
1) lib/reference-context.js: меню-слой RAG — блюда заведения (примеры +
   добавленные менеджером из localStorage) скорятся по вопросу
   (стемминг «салата»→«салат», «супов»→«супы»; матч по началу слов —
   «краб» находит «крабовую», но не «скраб»; аллергены в индексе:
   «в каких блюдах свинина» находит сало в крабовой котлете). Топ-2
   карточки блюда (состав, аллергены, важно, сочетание) уходят модели;
   при меню-вопросе справочник ужимается до одной главы. Смоук: 7/7.
2) Кнопка «Открыть меню»: инструкция учит модель маркеру [[go:menu]],
   ui/assistant.jsx знает подпись, App.jsx мапит menu → экран
   menuTrainer. Supabase по-прежнему не тронут.

# Дополнение 27 — курс «Гид по приложению» в Справочнике
Обучение по приложению выросло из пары строк в полный курс: 8 глав в
Справочнике (иконка-компас) — путь урок→тест→практика, Задания и банк
ошибок, тренажёры (Сборка/Меню/Диалоги/SOS), Справочник и поиск,
AI-наставник, График смен, Рейтинг и Книга отзывов. Восьмая глава
«Инструменты руководителя» (график-редактор с генератором и обменом,
онбординг новичков, AI-собеседование, аналитика, редактор контента)
помечена leaderOnly и видна ТОЛЬКО менеджерам/руководителям — фильтр
работает во всех четырёх точках: хаб Справочника, поиск, «глава дня»
(глава лидеров вне общего пула) и RAG ассистента.
Бонус архитектуры: ассистент теперь отвечает и на вопросы о самом
приложении («кто со мной в смене?», «где работа над ошибками?») — гид
попал в его базу автоматически. Заодно скоринг глав получил тот же
стемминг, что у меню («сменами» находит «смен»), а стоп-слова
пополнились приветствиями. Смоук: 13/13 кейсов, регрессов нет.
